
import Foundation

import UIKit
import AVFoundation

class LXFollowingController: UIViewController {
var namesPadding: Double? = 0.0
var isHighlights: Bool? = false




    @IBOutlet weak var open_player_avatar_imageView: UIImageView!
    @IBOutlet weak var open_player_name_label: UILabel!
    @IBOutlet weak var open_player_follow_button: UIButton!
    @IBOutlet weak var open_player_content_label: UILabel!
    @IBOutlet weak var tableView: UITableView!
    @IBOutlet weak var open_player_textFiled: UITextField!
    @IBOutlet weak var open_player_like_button: UIButton!
    @IBOutlet weak var pause_play_video_button: UIButton!
    
    var shortVideoData: [String: String] = [:]
    private var shortVideoPlayer: AVPlayer?
    private var shortVideoPlayerLayer: AVPlayerLayer?
    var report_black_view = HWVortexknightPortraitView()
    var commentList: [[String: String]] = []

@discardableResult
 func dispatchBuyActivateGuideServerServiceTableView(dynamic_dqVideo: [Any]!) -> UITableView! {
    var chatsk: String! = String(cString: [116,102,100,116,0], encoding: .utf8)!
    _ = chatsk
    var handshakek: [String: Any]! = [String(cString: [111,110,118,101,114,115,97,116,105,111,110,0], encoding: .utf8)!:UILabel()]
   withUnsafeMutablePointer(to: &handshakek) { pointer in
          _ = pointer.pointee
   }
      chatsk = "\(3 - chatsk.count)"
      handshakek = ["\(handshakek.count)": 2]
     var passwordPortrait: UIImageView! = UIImageView(image:UIImage(named:String(cString: [115,99,101,110,97,114,105,111,0], encoding: .utf8)!), highlightedImage:UIImage(named:String(cString: [109,111,110,111,115,112,97,99,101,100,0], encoding: .utf8)!))
     var register_s4Titan: String! = String(cString: [101,98,109,108,110,117,109,0], encoding: .utf8)!
     let bossfightFrostblade: Int = 83
    var framebuffersNonnullssrcOverwrite:UITableView! = UITableView()
    passwordPortrait.alpha = 0.5;
    passwordPortrait.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    passwordPortrait.frame = CGRect(x: 140, y: 152, width: 0, height: 0)
    passwordPortrait.animationRepeatCount = 0
    passwordPortrait.image = UIImage(named:String(cString: [101,109,98,101,114,0], encoding: .utf8)!)
    passwordPortrait.contentMode = .scaleAspectFit
    
    framebuffersNonnullssrcOverwrite.frame = CGRect(x: 297, y: 149, width: 0, height: 0)
    framebuffersNonnullssrcOverwrite.alpha = 0.0;
    framebuffersNonnullssrcOverwrite.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    framebuffersNonnullssrcOverwrite.dataSource = nil
    framebuffersNonnullssrcOverwrite.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    framebuffersNonnullssrcOverwrite.delegate = nil

    
    return framebuffersNonnullssrcOverwrite

}





    
    @objc private func shortVideoLoopReplay() {

         var scamCheckpoints: UITableView! = dispatchBuyActivateGuideServerServiceTableView(dynamic_dqVideo:[63, 15, 55])

      if scamCheckpoints != nil {
          let scamCheckpoints_tag = scamCheckpoints.tag
          self.view.addSubview(scamCheckpoints)
      }

withUnsafeMutablePointer(to: &scamCheckpoints) { pointer in
        _ = pointer.pointee
}


       var applei: Bool = true
   withUnsafeMutablePointer(to: &applei) { pointer in
          _ = pointer.pointee
   }
    var mapupdateG: Float = 3.0
      mapupdateG += (Float((applei ? 2 : 4) / (Swift.max(Int(mapupdateG > 29875662.0 || mapupdateG < -29875662.0 ? 26.0 : mapupdateG), 3))))

      applei = 78.17 == mapupdateG
        shortVideoPlayer?.seek(to: .zero)
        shortVideoPlayer?.play()
    }

@discardableResult
 func dispatchInstanceDurationPopularImageView(reportPrivacy: Bool, expiryCreator: Double, coordinatordispatcherHighscore: Double) -> UIImageView! {
    var filenameK: Bool = true
    var publisher3: String! = String(cString: [99,104,101,99,107,109,109,0], encoding: .utf8)!
    _ = publisher3
   while (filenameK) {
       var l_viewM: String! = String(cString: [115,110,111,119,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &l_viewM) { pointer in
             _ = pointer.pointee
      }
      repeat {
         l_viewM.append("\(3 >> (Swift.min(2, l_viewM.count)))")
         if 1462480 == l_viewM.count {
            break
         }
      } while (1462480 == l_viewM.count) && (l_viewM.hasSuffix("\(l_viewM.count)"))
         l_viewM.append("\(l_viewM.count)")
      for _ in 0 ..< 2 {
         l_viewM.append("\(l_viewM.count ^ 3)")
      }
      publisher3 = "\(l_viewM.count * 2)"
      break
   }
     let register_90Table: Float = 30.0
     var sentSkin: [Any]! = [true]
     let completelyTarget: String! = String(cString: [110,117,108,108,112,97,99,107,101,116,0], encoding: .utf8)!
     let optionsBonus: Int = 15
    var unemptyCluster: UIImageView! = UIImageView()
    unemptyCluster.frame = CGRect(x: 277, y: 302, width: 0, height: 0)
    unemptyCluster.alpha = 0.0;
    unemptyCluster.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unemptyCluster.image = UIImage(named:String(cString: [105,109,109,101,114,115,105,118,101,0], encoding: .utf8)!)
    unemptyCluster.contentMode = .scaleAspectFit
    unemptyCluster.animationRepeatCount = 7

    
    return unemptyCluster

}





    
    private func setupShortVideoPlayer() {

         var secondsAtacenter: UIImageView! = dispatchInstanceDurationPopularImageView(reportPrivacy:false, expiryCreator:96.0, coordinatordispatcherHighscore:27.0)

      if secondsAtacenter != nil {
          let secondsAtacenter_tag = secondsAtacenter.tag
          self.view.addSubview(secondsAtacenter)
      }
      else {
          print("secondsAtacenter is nil")      }

withUnsafeMutablePointer(to: &secondsAtacenter) { pointer in
        _ = pointer.pointee
}


       var delegate_v4: String! = String(cString: [99,111,100,101,102,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      delegate_v4.append("\(delegate_v4.count - 3)")
   }

        try? AVAudioSession.sharedInstance().setCategory(.playback)
        try? AVAudioSession.sharedInstance().setActive(true)
        
        guard let filename = shortVideoData["streamplayback_video_filename_buffering"],
              let vortexknight = Bundle.main.path(forResource: filename, ofType: "mp4") else { return }
        let videoo = URL(fileURLWithPath: vortexknight)
        shortVideoPlayer = AVPlayer(url: videoo)
        
        let blacklist = AVPlayerLayer(player: shortVideoPlayer)
        blacklist.frame = view.bounds
        blacklist.videoGravity = .resizeAspectFill
        view.layer.insertSublayer(blacklist, at: 0)
        shortVideoPlayerLayer = blacklist
        
        NotificationCenter.default.addObserver(self, selector: #selector(shortVideoLoopReplay), name: .AVPlayerItemDidPlayToEndTime, object: shortVideoPlayer?.currentItem)
        shortVideoPlayer?.play()
    }

    
    override func viewDidLayoutSubviews() {
       var realmd: [String: Any]! = [String(cString: [97,110,121,0], encoding: .utf8)!:UILabel()]
   while (realmd.count <= realmd.keys.count) {
      realmd = ["\(realmd.keys.count)": realmd.count]
      break
   }

        super.viewDidLayoutSubviews()
        shortVideoPlayerLayer?.frame = view.bounds
    }

@discardableResult
 func observeSignStoreLongitudeStyleDisengage(sessionRestore: Double) -> Bool {
    var restoreE: Float = 0.0
    var purchasesh: Bool = false
    var codew: Bool = true
   if purchasesh {
      restoreE += (Float((codew ? 2 : 1) + (purchasesh ? 2 : 1)))
   }
      purchasesh = restoreE > 29.83
       var followI: [Any]! = [String(cString: [105,115,115,117,101,114,115,0], encoding: .utf8)!, String(cString: [100,101,103,114,97,100,101,100,0], encoding: .utf8)!]
          var drift7: Double = 1.0
         followI = [followI.count]
         drift7 -= (Double(Int(drift7 > 19832565.0 || drift7 < -19832565.0 ? 48.0 : drift7) * followI.count))
          var blackD: [Any]! = [85, 7, 15]
          var queuec: String! = String(cString: [118,108,111,103,0], encoding: .utf8)!
          var shakeX: Double = 5.0
         followI.append(blackD.count)
         queuec = "\(1)"
         shakeX += Double(queuec.count % 2)
         followI.append(1 - followI.count)
      purchasesh = followI.count >= 74
   return purchasesh

}





    
    
    @IBAction func processUserLikeActionForGameCommunityPostItem(_ sender: UIButton) {

         var flacMoves: Bool = observeSignStoreLongitudeStyleDisengage(sessionRestore:24.0)

      if flacMoves {
          print("ok")
      }

withUnsafeMutablePointer(to: &flacMoves) { pointer in
        _ = pointer.pointee
}


       var callg: String! = String(cString: [99,97,114,116,101,115,105,97,110,0], encoding: .utf8)!
    var blitzy: String! = String(cString: [116,119,105,108,105,103,104,116,0], encoding: .utf8)!
      blitzy.append("\(blitzy.count)")

   for _ in 0 ..< 2 {
      callg = "\(1 % (Swift.max(5, callg.count)))"
   }
        if sender.tag == 311 { 
            let sandbox = open_player_textFiled.text ?? ""
            guard !sandbox.isEmpty else {
                MOHDiscover.gameLoadingText("Please enter a comment", in: self.view)
                return
            }
            let photo = shortVideoData["streamplayback_video_filename_buffering"] ?? ""
            FXBlazerunner.shared.threadreply_postcomment_tovideo(photo, text: sandbox)
            open_player_textFiled.text = ""
            open_player_textFiled.resignFirstResponder()
            loadComments()
            if commentList.count > 0 {
                tableView.scrollToRow(at: IndexPath(row: commentList.count - 1, section: 0), at: .bottom, animated: true)
            }
            return
        }
        if sender.tag == 312 { 
            sender.isSelected = !sender.isSelected
            shortVideoPlayer?.isMuted = sender.isSelected
            return
        }
        if sender.tag == 313 { 
            let photo = shortVideoData["streamplayback_video_filename_buffering"] ?? ""
            if FXBlazerunner.shared.interactionpulse_isvideo_liked_signal(photo) {
                FXBlazerunner.shared.engagementdrop_unlikevideo_fromlist_churn(photo)
                sender.setBackgroundImage(UIImage(named: "passWildforgeVerificator"), for: .normal)
            } else {
                FXBlazerunner.shared.engagementboost_likevideo_tolist_retention(shortVideoData)
                sender.setBackgroundImage(UIImage(named: "qdmfndjsRaidRaid"), for: .normal)
            }
            return
        }
        if sender.tag == 318 { 
            if shortVideoPlayer?.rate == 0 {
                shortVideoPlayer?.play()
                pause_play_video_button.isSelected = false
                pause_play_video_button.setImage(UIImage(named: ""), for: .normal)
            } else {
                shortVideoPlayer?.pause()
                pause_play_video_button.isSelected = true
                pause_play_video_button.setImage(UIImage(named: "mywalletPopularPurple"), for: .normal)
            }
        }
    }

@discardableResult
 func playInsightTotalAfterBackdropTrialTableView(invalidDesc: String!, type_60Content: Double, nicknameFind: Double) -> UITableView! {
    var alt7: [String: Any]! = [String(cString: [116,105,108,101,109,107,97,0], encoding: .utf8)!:96, String(cString: [112,104,112,0], encoding: .utf8)!:39, String(cString: [99,111,118,101,114,0], encoding: .utf8)!:71]
    var tests: String! = String(cString: [102,117,110,103,105,98,108,101,115,0], encoding: .utf8)!
   if 1 == tests.count {
       var createB: String! = String(cString: [115,116,111,114,101,120,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &createB) { pointer in
             _ = pointer.pointee
      }
         createB = "\(createB.count)"
         createB.append("\(3 << (Swift.min(3, createB.count)))")
      repeat {
          var completioni: Bool = true
          var gamej: String! = String(cString: [100,105,115,112,111,115,97,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &gamej) { pointer in
    
         }
         createB = "\(((completioni ? 5 : 2)))"
         gamej.append("\(1)")
         if 4278288 == createB.count {
            break
         }
      } while (4278288 == createB.count) && (createB.count == 2)
      tests.append("\(createB.count)")
   }
   if (tests.count << (Swift.min(labs(3), 3))) < 5 && 3 < (alt7.count << (Swift.min(tests.count, 4))) {
      alt7[tests] = alt7.count ^ tests.count
   }
     var likedInput: Double = 23.0
     let chatsSign: String! = String(cString: [115,108,105,99,101,116,104,114,101,97,100,0], encoding: .utf8)!
     let vortexknightHjbewhj: Double = 39.0
    var pngContrained:UITableView! = UITableView()
    pngContrained.delegate = nil
    pngContrained.dataSource = nil
    pngContrained.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pngContrained.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pngContrained.alpha = 0.4
    pngContrained.frame = CGRect(x: 119, y: 93, width: 0, height: 0)

    
    return pngContrained

}





    
    func setupUIReportBlackView() {

         var smilingBadreq: UITableView! = playInsightTotalAfterBackdropTrialTableView(invalidDesc:String(cString: [117,116,120,111,0], encoding: .utf8)!, type_60Content:63.0, nicknameFind:46.0)

      if smilingBadreq != nil {
          let smilingBadreq_tag = smilingBadreq.tag
          self.view.addSubview(smilingBadreq)
      }
      else {
          print("smilingBadreq is nil")      }

withUnsafeMutablePointer(to: &smilingBadreq) { pointer in
    
}


       var vortexknightX: [String: Any]! = [String(cString: [117,110,102,101,116,99,104,0], encoding: .utf8)!:UILabel()]
   if vortexknightX["\(vortexknightX.count)"] != nil {
      vortexknightX["\(vortexknightX.count)"] = 2
   }

        report_black_view = UINib(nibName: "HWVortexknightPortraitView", bundle: nil).instantiate(withOwner: self, options: nil).first as! HWVortexknightPortraitView
        report_black_view.delegate = self
        view.addSubview(report_black_view)
        report_black_view.frame = CGRect(x: 0, y: 1200, width: UIScreen.main.bounds.width, height: UIScreen.main.bounds.height)
    }

    
    override func viewDidLoad() {
       var updatedf: String! = String(cString: [115,116,114,105,100,101,98,0], encoding: .utf8)!
   if updatedf == String(cString:[109,0], encoding: .utf8)! {
      updatedf = "\(3 - updatedf.count)"
   }

        super.viewDidLoad()
        open_player_avatar_imageView.layer.borderColor = UIColor.white.cgColor
        let select = shortVideoData["uploadercreator_video_publishernickname_channel"] ?? ""
        if FXBlazerunner.shared.handshakeresponse_isfriendrequest_sent_already(select) {
            open_player_follow_button.alpha = 0.55
        }
        
        tableView.delegate = self
        tableView.dataSource = self
        tableView.register(UINib(nibName: "SMABlackCell", bundle: nil), forCellReuseIdentifier: "online")
        
        if let avatarName = shortVideoData["profileiconbadge_video_publisheravatar_verification"] {
            open_player_avatar_imageView.image = UIImage(named: avatarName)
        }
        open_player_name_label.text = shortVideoData["uploadercreator_video_publishernickname_channel"]
        open_player_content_label.text = shortVideoData["captionsubtitle_video_description_overlay"]
        
        setupShortVideoPlayer()
        setupUIReportBlackView()
        loadComments()
        
        let photo = shortVideoData["streamplayback_video_filename_buffering"] ?? ""
        if FXBlazerunner.shared.interactionpulse_isvideo_liked_signal(photo) {
            open_player_like_button.setBackgroundImage(UIImage(named: "qdmfndjsRaidRaid"), for: .normal)
        }
    }
    
    deinit {
        NotificationCenter.default.removeObserver(self)
    }

@discardableResult
 func cancellationMediumShape() -> String! {
    var q_imageC: [Any]! = [String(cString: [108,111,111,107,97,104,101,97,100,0], encoding: .utf8)!, String(cString: [98,108,105,116,0], encoding: .utf8)!, String(cString: [99,114,108,100,112,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &q_imageC) { pointer in
    
   }
    var roome: String! = String(cString: [115,107,101,121,108,105,115,116,0], encoding: .utf8)!
    _ = roome
    var buyK: String! = String(cString: [105,110,100,101,120,105,110,103,0], encoding: .utf8)!
    _ = buyK
   repeat {
      buyK = "\((buyK == (String(cString:[104,0], encoding: .utf8)!) ? q_imageC.count : buyK.count))"
      if (String(cString:[103,97,101,100,51,95,115,97,106,0], encoding: .utf8)!) == buyK {
         break
      }
   } while ((buyK.count | 4) <= 4) && ((String(cString:[103,97,101,100,51,95,115,97,106,0], encoding: .utf8)!) == buyK)
       var needsA: Float = 4.0
       var highscorej: Double = 2.0
          var message0: String! = String(cString: [97,118,100,99,116,0], encoding: .utf8)!
         needsA -= (Float(Int(highscorej > 3232447.0 || highscorej < -3232447.0 ? 84.0 : highscorej)))
         message0.append("\((Int(highscorej > 10452788.0 || highscorej < -10452788.0 ? 62.0 : highscorej)))")
         highscorej -= (Double(Int(needsA > 146611805.0 || needsA < -146611805.0 ? 16.0 : needsA) ^ Int(highscorej > 64304578.0 || highscorej < -64304578.0 ? 91.0 : highscorej)))
         needsA /= Swift.max(Float(3), 5)
         highscorej += (Double(Int(highscorej > 325607894.0 || highscorej < -325607894.0 ? 99.0 : highscorej) / 3))
         highscorej += (Double(Int(needsA > 228438093.0 || needsA < -228438093.0 ? 5.0 : needsA)))
          var blacku: String! = String(cString: [115,105,109,112,108,101,0], encoding: .utf8)!
          var fans7: Bool = true
         highscorej += (Double(Int(needsA > 263121339.0 || needsA < -263121339.0 ? 97.0 : needsA) << (Swift.min(5, labs(3)))))
         blacku.append("\((Int(highscorej > 63195698.0 || highscorej < -63195698.0 ? 74.0 : highscorej)))")
         fans7 = needsA <= 67.10 || highscorej <= 67.10
      buyK = "\(1)"
       var loadingY: String! = String(cString: [102,116,118,102,111,108,100,101,114,111,112,101,110,0], encoding: .utf8)!
       var friend_7n: [Any]! = [51, 88]
         loadingY = "\(friend_7n.count)"
      if 2 >= (loadingY.count << (Swift.min(labs(3), 1))) && (loadingY.count << (Swift.min(5, friend_7n.count))) >= 3 {
         friend_7n.append(loadingY.count % (Swift.max(6, friend_7n.count)))
      }
      while (!loadingY.hasPrefix("\(friend_7n.count)")) {
         loadingY.append("\((loadingY == (String(cString:[100,0], encoding: .utf8)!) ? loadingY.count : friend_7n.count))")
         break
      }
      if (3 + friend_7n.count) >= 4 {
         loadingY = "\(friend_7n.count / 1)"
      }
      while (friend_7n.count > loadingY.count) {
          var clashZ: [String: Any]! = [String(cString: [113,117,97,110,116,115,0], encoding: .utf8)!:29]
          var typesV: String! = String(cString: [97,108,108,111,99,97,116,105,111,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &typesV) { pointer in
                _ = pointer.pointee
         }
         friend_7n = [typesV.count + friend_7n.count]
         clashZ = ["\(clashZ.count)": typesV.count >> (Swift.min(labs(3), 1))]
         break
      }
         friend_7n = [friend_7n.count]
      roome.append("\(loadingY.count)")
   return roome

}





    
    @IBAction func openPlayerVideoReturnClick(_ sender: Any) {

         let matxAuthenticator: String! = cancellationMediumShape()

      let matxAuthenticator_len = matxAuthenticator?.count ?? 0
      if matxAuthenticator == "localized" {
              print(matxAuthenticator)
      }

_ = matxAuthenticator


       var with_w5: String! = String(cString: [119,114,105,116,101,114,0], encoding: .utf8)!
    var lastJ: Bool = false
      with_w5.append("\(with_w5.count)")
       var m_titleM: String! = String(cString: [115,101,110,115,105,116,105,118,101,0], encoding: .utf8)!
       _ = m_titleM
       var homef: String! = String(cString: [115,105,116,101,0], encoding: .utf8)!
      if !homef.hasSuffix(m_titleM) {
         m_titleM = "\(m_titleM.count)"
      }
      if 2 <= homef.count {
          var shadowB: String! = String(cString: [99,111,112,121,116,101,115,116,0], encoding: .utf8)!
          var headerW: String! = String(cString: [107,101,99,99,97,107,0], encoding: .utf8)!
          var dismiss0: String! = String(cString: [113,119,111,114,100,0], encoding: .utf8)!
          var rechagre4: String! = String(cString: [116,119,111,119,97,121,0], encoding: .utf8)!
         m_titleM = "\((headerW == (String(cString:[53,0], encoding: .utf8)!) ? headerW.count : dismiss0.count))"
         shadowB = "\(rechagre4.count)"
         rechagre4.append("\(homef.count)")
      }
      repeat {
          var frostu: Double = 5.0
         homef = "\(3)"
         frostu /= Swift.max(1, (Double(Int(frostu > 188445376.0 || frostu < -188445376.0 ? 18.0 : frostu) * 2)))
         if (String(cString:[97,115,97,109,116,0], encoding: .utf8)!) == homef {
            break
         }
      } while (3 == m_titleM.count && homef != String(cString:[73,0], encoding: .utf8)!) && ((String(cString:[97,115,97,109,116,0], encoding: .utf8)!) == homef)
      repeat {
         homef = "\(2 + homef.count)"
         if (String(cString:[56,111,53,49,102,54,0], encoding: .utf8)!) == homef {
            break
         }
      } while ((String(cString:[56,111,53,49,102,54,0], encoding: .utf8)!) == homef) && (m_titleM != String(cString:[71,0], encoding: .utf8)!)
         m_titleM = "\(m_titleM.count)"
          var contentu: Int = 1
         m_titleM.append("\((m_titleM == (String(cString:[102,0], encoding: .utf8)!) ? m_titleM.count : contentu))")
      lastJ = 100 > homef.count

        self.navigationController?.popViewController(animated: true)
    }

    
    
    @IBAction func requestUserFollowOrUnfollowTargetCommunityCreator(_ sender: UIButton) {
       var bigs: [String: Any]! = [String(cString: [121,101,115,110,111,0], encoding: .utf8)!:19, String(cString: [108,97,121,101,114,0], encoding: .utf8)!:3, String(cString: [115,97,109,112,0], encoding: .utf8)!:52]
   for _ in 0 ..< 2 {
      bigs["\(bigs.count)"] = bigs.count >> (Swift.min(2, bigs.count))
   }

        if sender.tag == 511 {
            UIView.animate(withDuration: 0.31) {
                self.report_black_view.frame = CGRect(x: 0, y: 0, width: UIScreen.main.bounds.width, height: UIScreen.main.bounds.height)
            }
            return
        }
        if sender.tag == 512 {
            let localized = shortVideoData["streamplayback_video_filename_buffering"] ?? ""
            guard let videoPath = Bundle.main.path(forResource: localized, ofType: "mp4") else { return }
            let videoo = URL(fileURLWithPath: videoPath)
            let willController = UIActivityViewController(activityItems: [videoo], applicationActivities: nil)
            present(willController, animated: true)
            return
        }
        if sender.tag == 513 { 
            let select = shortVideoData["uploadercreator_video_publishernickname_channel"] ?? ""
            let wrapper = shortVideoData["profileiconbadge_video_publisheravatar_verification"] ?? ""
            if FXBlazerunner.shared.handshakeresponse_isfriendrequest_sent_already(select) {
                MOHDiscover.gameLoadingText("Friend request already sent", in: self.view)
            } else {
                FXBlazerunner.shared.socialoutreach_sendfriendrequest_connection(select, avatar: wrapper, message: "Friend request sent, waiting for response")
                MOHDiscover.gameLoadingSuccess("Friend request sent, waiting for response", in: self.view)
                open_player_follow_button.alpha = 0.55
            }
            return
        }
        if sender.tag == 514 { 
            let attachController = QQALoadingController()
            attachController.userData = [
                "nickname": shortVideoData["uploadercreator_video_publishernickname_channel"] ?? "",
                "avatar": shortVideoData["profileiconbadge_video_publisheravatar_verification"] ?? ""
            ]
            navigationController?.pushViewController(attachController, animated: true)
            return
        }
        
    }

@discardableResult
 func launchAddressGameReceiveTextPriceButton(shadowManager: String!) -> UIButton! {
    var fromI: String! = String(cString: [109,100,99,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &fromI) { pointer in
    
   }
    var userT: String! = String(cString: [101,100,105,116,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
       var waveQ: Double = 2.0
         waveQ /= Swift.max((Double(Int(waveQ > 256584403.0 || waveQ < -256584403.0 ? 31.0 : waveQ))), 2)
          var games: String! = String(cString: [98,111,111,108,0], encoding: .utf8)!
          var pairz: Bool = false
          var loadingl: String! = String(cString: [115,112,97,99,101,114,115,0], encoding: .utf8)!
         waveQ /= Swift.max((Double((pairz ? 3 : 2))), 1)
         games.append("\(1)")
         loadingl = "\(loadingl.count)"
         waveQ /= Swift.max((Double(Int(waveQ > 157035095.0 || waveQ < -157035095.0 ? 84.0 : waveQ))), 1)
      fromI = "\(userT.count)"
   }
       var insight5: String! = String(cString: [97,108,105,103,110,101,100,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         insight5 = "\(3 & insight5.count)"
      }
      repeat {
          var purchasek: String! = String(cString: [117,110,101,109,98,101,100,0], encoding: .utf8)!
          var pickerd: String! = String(cString: [97,118,99,111,100,101,99,0], encoding: .utf8)!
          var typesH: [String: Any]! = [String(cString: [118,97,114,108,101,110,0], encoding: .utf8)!:64]
         insight5 = "\(purchasek.count / (Swift.max(insight5.count, 4)))"
         pickerd = "\(1 >> (Swift.min(5, insight5.count)))"
         typesH = ["\(typesH.values.count)": (purchasek == (String(cString:[113,0], encoding: .utf8)!) ? typesH.keys.count : purchasek.count)]
         if insight5.count == 1896377 {
            break
         }
      } while (insight5.count >= 4) && (insight5.count == 1896377)
         insight5 = "\(((String(cString:[113,0], encoding: .utf8)!) == insight5 ? insight5.count : insight5.count))"
      userT = "\(insight5.count - 3)"
     var wrapperItems: Float = 72.0
     var cellSave: UILabel! = UILabel()
     let agreeString: UIImageView! = UIImageView(image:UIImage(named:String(cString: [99,111,108,0], encoding: .utf8)!))
     var identifierAll: Double = 69.0
    var mbsegmentDownloadTrap:UIButton! = UIButton(frame:CGRect.zero)
    mbsegmentDownloadTrap.alpha = 0.1;
    mbsegmentDownloadTrap.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    mbsegmentDownloadTrap.frame = CGRect(x: 78, y: 83, width: 0, height: 0)
    mbsegmentDownloadTrap.setBackgroundImage(UIImage(named:String(cString: [116,111,116,97,108,0], encoding: .utf8)!), for: .normal)
    mbsegmentDownloadTrap.titleLabel?.font = UIFont.systemFont(ofSize:14)
    mbsegmentDownloadTrap.setImage(UIImage(named:String(cString: [115,117,99,99,101,115,115,0], encoding: .utf8)!), for: .normal)
    mbsegmentDownloadTrap.setTitle("", for: .normal)
    cellSave.frame = CGRect(x: 201, y: 172, width: 0, height: 0)
    cellSave.alpha = 0.6;
    cellSave.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    cellSave.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    cellSave.textAlignment = .left
    cellSave.font = UIFont.systemFont(ofSize:12)
    cellSave.text = ""
    
    agreeString.frame = CGRect(x: 256, y: 301, width: 0, height: 0)
    agreeString.alpha = 0.0;
    agreeString.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    agreeString.contentMode = .scaleAspectFit
    agreeString.animationRepeatCount = 7
    agreeString.image = UIImage(named:String(cString: [112,114,111,100,117,99,116,0], encoding: .utf8)!)
    

    
    return mbsegmentDownloadTrap

}





    
    func loadComments() {

         let ripemdCmake: UIButton! = launchAddressGameReceiveTextPriceButton(shadowManager:String(cString: [112,97,114,116,105,99,108,101,0], encoding: .utf8)!)

      if ripemdCmake != nil {
          let ripemdCmake_tag = ripemdCmake.tag
          self.view.addSubview(ripemdCmake)
      }
      else {
          print("ripemdCmake is nil")      }

_ = ripemdCmake


       var register_j5n: Int = 2
   repeat {
       var editk: Double = 2.0
       _ = editk
       var durationY: [Any]! = [true]
       _ = durationY
       var retrievedA: Float = 0.0
       var staro: String! = String(cString: [109,105,120,112,97,110,101,108,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &staro) { pointer in
    
      }
       var momentn: String! = String(cString: [97,115,98,100,0], encoding: .utf8)!
       _ = momentn
      for _ in 0 ..< 2 {
         durationY.append(2 % (Swift.max(7, momentn.count)))
      }
         editk -= (Double(Int(retrievedA > 100675988.0 || retrievedA < -100675988.0 ? 88.0 : retrievedA)))
      if 2 > (staro.count - 5) && (staro.count - 5) > 3 {
         staro.append("\((Int(editk > 230509083.0 || editk < -230509083.0 ? 63.0 : editk) >> (Swift.min(staro.count, 1))))")
      }
      repeat {
         retrievedA /= Swift.max(Float(3), 4)
         if 1820592.0 == retrievedA {
            break
         }
      } while (3.10 == (4.85 - retrievedA) || 5 == (Int(retrievedA > 88874327.0 || retrievedA < -88874327.0 ? 96.0 : retrievedA) - 5)) && (1820592.0 == retrievedA)
       var noteV: [String: Any]! = [String(cString: [103,114,97,112,104,105,99,115,0], encoding: .utf8)!:89, String(cString: [111,112,116,105,109,97,108,0], encoding: .utf8)!:55, String(cString: [114,97,100,98,103,0], encoding: .utf8)!:89]
       _ = noteV
      repeat {
          var commentV: Double = 3.0
          var savedu: Float = 0.0
          var priceV: Int = 1
          var cancelj: Float = 2.0
         staro = "\((Int(savedu > 327205582.0 || savedu < -327205582.0 ? 74.0 : savedu) << (Swift.min(5, labs(1)))))"
         commentV -= (Double(durationY.count ^ Int(editk > 102654366.0 || editk < -102654366.0 ? 22.0 : editk)))
         priceV /= Swift.max((Int(retrievedA > 20451337.0 || retrievedA < -20451337.0 ? 25.0 : retrievedA) >> (Swift.min(5, labs(2)))), 4)
         cancelj -= Float(staro.count)
         if staro.count == 2656365 {
            break
         }
      } while (staro.count == 2656365) && (staro.contains("\(retrievedA)"))
      while (5 > noteV.values.count) {
         staro = "\((Int(editk > 304480933.0 || editk < -304480933.0 ? 21.0 : editk)))"
         break
      }
         momentn.append("\(noteV.count ^ 1)")
          var pravicyu: Double = 5.0
          var enterf: String! = String(cString: [97,108,116,0], encoding: .utf8)!
          var dfbwE: [String: Any]! = [String(cString: [100,99,97,109,97,116,104,0], encoding: .utf8)!:10, String(cString: [114,101,112,114,101,115,101,110,116,97,116,105,111,110,0], encoding: .utf8)!:59, String(cString: [102,97,100,101,0], encoding: .utf8)!:42]
         momentn = "\(1 & noteV.keys.count)"
         pravicyu -= Double(2)
         enterf.append("\(((String(cString:[111,0], encoding: .utf8)!) == enterf ? enterf.count : dfbwE.keys.count))")
         dfbwE = ["\(dfbwE.count)": (Int(pravicyu > 59174266.0 || pravicyu < -59174266.0 ? 3.0 : pravicyu) * dfbwE.count)]
      while ((Int(retrievedA > 217549685.0 || retrievedA < -217549685.0 ? 97.0 : retrievedA) + 2) == 3 || (Float(staro.count) + retrievedA) == 2.87) {
         staro = "\(momentn.count)"
         break
      }
      for _ in 0 ..< 1 {
          var friendsM: Double = 3.0
          var currento: String! = String(cString: [112,114,111,103,114,97,109,115,0], encoding: .utf8)!
          var filterm: Double = 2.0
          _ = filterm
          var restoreB: [String: Any]! = [String(cString: [99,104,111,105,99,101,115,0], encoding: .utf8)!:String(cString: [115,117,112,101,114,115,101,116,0], encoding: .utf8)!]
         momentn.append("\((Int(filterm > 16293681.0 || filterm < -16293681.0 ? 34.0 : filterm) ^ 1))")
         friendsM -= Double(momentn.count)
         currento.append("\((Int(retrievedA > 132187953.0 || retrievedA < -132187953.0 ? 71.0 : retrievedA) * 3))")
         restoreB = ["\(restoreB.values.count)": ((String(cString:[116,0], encoding: .utf8)!) == momentn ? restoreB.keys.count : momentn.count)]
      }
         staro = "\((1 ^ Int(editk > 142402005.0 || editk < -142402005.0 ? 3.0 : editk)))"
         staro.append("\(noteV.keys.count % (Swift.max(3, 5)))")
       var clipu: String! = String(cString: [105,110,116,101,114,108,101,97,118,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &clipu) { pointer in
    
      }
          var requestU: [String: Any]! = [String(cString: [114,101,102,105,108,108,0], encoding: .utf8)!:String(cString: [99,111,110,118,101,114,103,101,110,99,101,0], encoding: .utf8)!]
          _ = requestU
          var call1: Double = 2.0
         withUnsafeMutablePointer(to: &call1) { pointer in
                _ = pointer.pointee
         }
          var serviceW: String! = String(cString: [109,97,114,107,0], encoding: .utf8)!
         staro = "\((2 | Int(retrievedA > 245575496.0 || retrievedA < -245575496.0 ? 7.0 : retrievedA)))"
         requestU[serviceW] = (serviceW == (String(cString:[95,0], encoding: .utf8)!) ? Int(editk > 147819358.0 || editk < -147819358.0 ? 77.0 : editk) : serviceW.count)
         call1 += Double(serviceW.count)
         clipu = "\(noteV.values.count / (Swift.max(momentn.count, 9)))"
      register_j5n -= momentn.count | 1
      if register_j5n == 3290951 {
         break
      }
   } while (register_j5n == 3290951) && (register_j5n >= register_j5n)

        let photo = shortVideoData["streamplayback_video_filename_buffering"] ?? ""
        commentList = FXBlazerunner.shared.threadhistory_videocomments_retrieve(photo)
        tableView.reloadData()
    }
    
    
}

extension LXFollowingController: UITableViewDelegate, UITableViewDataSource {

@discardableResult
 func rateAfterComponent(indexEdit: [String: Any]!, nightMilliseconds: Double, glowPair: Double) -> String! {
    var neonv: String! = String(cString: [97,117,116,111,109,97,116,105,99,97,108,108,121,0], encoding: .utf8)!
    _ = neonv
    var crimsonarkP: [Any]! = [[String(cString: [116,111,108,111,119,101,114,0], encoding: .utf8)!, String(cString: [100,114,105,118,105,110,103,0], encoding: .utf8)!, String(cString: [97,108,112,105,110,101,0], encoding: .utf8)!]]
    var comboD: String! = String(cString: [100,114,105,118,101,110,0], encoding: .utf8)!
   while (!neonv.hasSuffix("\(crimsonarkP.count)")) {
      crimsonarkP.append(3)
      break
   }
      neonv = "\((comboD == (String(cString:[90,0], encoding: .utf8)!) ? comboD.count : crimsonarkP.count))"
   for _ in 0 ..< 3 {
      neonv = "\(crimsonarkP.count)"
   }
   return neonv

}





    
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {

         let objectCropping: String! = rateAfterComponent(indexEdit:[String(cString: [112,114,101,115,101,110,116,105,110,103,0], encoding: .utf8)!:String(cString: [109,98,99,115,0], encoding: .utf8)!, String(cString: [112,116,114,109,97,112,0], encoding: .utf8)!:String(cString: [99,111,109,98,105,110,101,0], encoding: .utf8)!, String(cString: [112,101,117,107,101,114,0], encoding: .utf8)!:String(cString: [102,105,108,101,104,101,97,100,101,114,0], encoding: .utf8)!], nightMilliseconds:74.0, glowPair:15.0)

      print(objectCropping)
      let objectCropping_len = objectCropping?.count ?? 0

_ = objectCropping


       var findT: Double = 4.0
    var yellowB: String! = String(cString: [100,111,110,101,0], encoding: .utf8)!
    var watchdog2: String! = String(cString: [99,111,112,121,0], encoding: .utf8)!
    _ = watchdog2
      watchdog2 = "\((Int(findT > 135519458.0 || findT < -135519458.0 ? 5.0 : findT) - 2))"

      watchdog2.append("\(2 & watchdog2.count)")
        let performCell = tableView.dequeueReusableCell(withIdentifier: "online", for: indexPath) as! SMABlackCell
   repeat {
      yellowB.append("\((Int(findT > 300389746.0 || findT < -300389746.0 ? 85.0 : findT) ^ yellowB.count))")
      if yellowB.count == 4102539 {
         break
      }
   } while (yellowB.count == 4102539) && (1 < (yellowB.count | 1) && 1 < (Int(findT > 249658286.0 || findT < -249658286.0 ? 32.0 : findT) / (Swift.max(yellowB.count, 8))))
        performCell.backgroundColor = UIColor.clear
      yellowB = "\(3)"
        performCell.selectionStyle = .none
        
        let scheduler = commentList[indexPath.row]
        performCell.comment_user_name_label.text = (scheduler["nickname"] ?? "") + " :"
        performCell.comment_content_label.text = scheduler["text"]
        
        let filtered = scheduler["sender"] == "me"
        performCell.delete_comment_button.isHidden = !filtered
        performCell.delete_comment_button.tag = indexPath.row
        performCell.delete_comment_button.removeTarget(self, action: #selector(deleteCommentAction(_:)), for: .touchUpInside)
        performCell.delete_comment_button.addTarget(self, action: #selector(deleteCommentAction(_:)), for: .touchUpInside)
        
        return performCell
    }

@discardableResult
 func generateAfterFollowingInputGreenImageView(labelMine: String!) -> UIImageView! {
    var transactionsK: Bool = false
    _ = transactionsK
    var builder7: [Any]! = [8, 70]
   if 1 < (3 * builder7.count) && 3 < builder7.count {
       var recordr: Int = 2
       var cellX: Bool = false
      withUnsafeMutablePointer(to: &cellX) { pointer in
             _ = pointer.pointee
      }
         recordr -= ((cellX ? 5 : 3) & recordr)
      for _ in 0 ..< 2 {
          var internal_b8m: Double = 0.0
          var nicknameW: Float = 3.0
          var gameplay7: Double = 5.0
          var noteF: Double = 0.0
          _ = noteF
          var delete_qt: Bool = true
          _ = delete_qt
         cellX = !cellX
         internal_b8m /= Swift.max(3, (Double(Int(gameplay7 > 110609284.0 || gameplay7 < -110609284.0 ? 61.0 : gameplay7) * (cellX ? 5 : 3))))
         nicknameW -= (Float(2 + Int(nicknameW > 280334227.0 || nicknameW < -280334227.0 ? 5.0 : nicknameW)))
         gameplay7 += (Double(Int(noteF > 362730638.0 || noteF < -362730638.0 ? 8.0 : noteF) - recordr))
         noteF -= Double(2)
         delete_qt = gameplay7 < 7.83
      }
      repeat {
         recordr &= 3
         if 2923180 == recordr {
            break
         }
      } while (recordr == 3 || (recordr & 3) == 4) && (2923180 == recordr)
         recordr -= 1
         cellX = recordr >= 69
         cellX = recordr >= 5
      transactionsK = (builder7.contains { $0 as? Bool == transactionsK })
   }
      transactionsK = builder7.count >= 80 && !transactionsK
     let likedEdited: [Any]! = [72, 91, 64]
     var userFriends: Float = 83.0
    var basedLottieviewLink = UIImageView(frame:CGRect.zero)
    basedLottieviewLink.animationRepeatCount = 0
    basedLottieviewLink.image = UIImage(named:String(cString: [114,101,97,108,109,0], encoding: .utf8)!)
    basedLottieviewLink.contentMode = .scaleAspectFit
    basedLottieviewLink.frame = CGRect(x: 117, y: 152, width: 0, height: 0)
    basedLottieviewLink.alpha = 0.9;
    basedLottieviewLink.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return basedLottieviewLink

}





    
    @objc func deleteCommentAction(_ sender: UIButton) {

         let unmovedErrcode: UIImageView! = generateAfterFollowingInputGreenImageView(labelMine:String(cString: [102,111,114,119,97,114,100,105,110,103,0], encoding: .utf8)!)

      if unmovedErrcode != nil {
          let unmovedErrcode_tag = unmovedErrcode.tag
          self.view.addSubview(unmovedErrcode)
      }

_ = unmovedErrcode


       var costG: Double = 0.0
      costG += (Double(Int(costG > 105265313.0 || costG < -105265313.0 ? 12.0 : costG)))

        let state = sender.tag
        let photo = shortVideoData["streamplayback_video_filename_buffering"] ?? ""
        FXBlazerunner.shared.threadremove_deletecomment_fromvideo(photo, at: state)
        loadComments()
    }

    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
       var coordinatordispatcherO: [Any]! = [34, 24]
   for _ in 0 ..< 3 {
      coordinatordispatcherO.append(3)
   }

        return commentList.count
    }
}

extension LXFollowingController: AONBMReauestViper {

@discardableResult
 func userClickCornerWhenFitFrom() -> String! {
    var screenP: String! = String(cString: [101,116,101,114,110,105,116,121,0], encoding: .utf8)!
    var containerM: Double = 1.0
    var homeC: String! = String(cString: [116,101,115,116,98,105,116,0], encoding: .utf8)!
      screenP.append("\(homeC.count / 3)")
       var loungeF: String! = String(cString: [115,109,117,115,104,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &loungeF) { pointer in
    
      }
      while (loungeF != String(cString:[86,0], encoding: .utf8)!) {
          var confirmZ: Bool = false
          var debug8: String! = String(cString: [101,108,101,109,115,0], encoding: .utf8)!
         loungeF.append("\(1 ^ loungeF.count)")
         debug8.append("\(((String(cString:[98,0], encoding: .utf8)!) == debug8 ? (confirmZ ? 3 : 4) : debug8.count))")
         break
      }
      for _ in 0 ..< 2 {
         loungeF.append("\(1)")
      }
         loungeF = "\(loungeF.count - 1)"
      containerM /= Swift.max(Double(homeC.count), 3)
   while ((4.38 / (Swift.max(3, containerM))) <= 2.77) {
      homeC = "\((Int(containerM > 114880693.0 || containerM < -114880693.0 ? 18.0 : containerM) * 1))"
      break
   }
   return screenP

}






    func playReportBlackViewDelegateSuccess(rateLimitTag: Int) {

         let cachesizeOperators: String! = userClickCornerWhenFitFrom()

      let cachesizeOperators_len = cachesizeOperators?.count ?? 0
      print(cachesizeOperators)

_ = cachesizeOperators


       var edito: [Any]! = [87, 25]
       var v_titlej: String! = String(cString: [110,111,109,105,110,97,116,101,0], encoding: .utf8)!
       var orderJ: Float = 0.0
       _ = orderJ
       var detailsk: Double = 1.0
      repeat {
         orderJ += (Float(Int(detailsk > 193391099.0 || detailsk < -193391099.0 ? 63.0 : detailsk) << (Swift.min(labs(Int(orderJ > 211897315.0 || orderJ < -211897315.0 ? 22.0 : orderJ)), 1))))
         if 2715074.0 == orderJ {
            break
         }
      } while (2715074.0 == orderJ) && (3.1 < (orderJ - 1.53))
      if (Double(orderJ) + detailsk) < 2.6 && (2.6 + detailsk) < 1.64 {
          var blitzZ: String! = String(cString: [109,111,100,105,102,105,99,97,116,105,111,110,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &blitzZ) { pointer in
                _ = pointer.pointee
         }
          var barF: String! = String(cString: [102,116,118,100,111,99,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &barF) { pointer in
    
         }
         orderJ -= Float(blitzZ.count)
         barF.append("\(v_titlej.count << (Swift.min(labs(3), 4)))")
      }
          var scalen: String! = String(cString: [114,101,115,111,108,118,101,114,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &scalen) { pointer in
    
         }
         orderJ += (Float(Int(detailsk > 245679378.0 || detailsk < -245679378.0 ? 57.0 : detailsk) << (Swift.min(2, labs(Int(orderJ > 80704551.0 || orderJ < -80704551.0 ? 99.0 : orderJ))))))
         scalen.append("\(1)")
      repeat {
          var recordo: Int = 2
          var portraitb: String! = String(cString: [116,114,117,115,116,0], encoding: .utf8)!
          var smirol: [Any]! = [73, 45, 10]
         v_titlej = "\(3 ^ smirol.count)"
         recordo ^= 2 * portraitb.count
         portraitb.append("\(v_titlej.count)")
         if v_titlej.count == 3296968 {
            break
         }
      } while ((orderJ / (Swift.max(Float(v_titlej.count), 7))) >= 5.91 && 1 >= (1 * v_titlej.count)) && (v_titlej.count == 3296968)
         orderJ -= (Float((String(cString:[50,0], encoding: .utf8)!) == v_titlej ? Int(orderJ > 27937808.0 || orderJ < -27937808.0 ? 88.0 : orderJ) : v_titlej.count))
      if 3.41 < (orderJ - 5.61) {
         orderJ += (Float(Int(orderJ > 349825950.0 || orderJ < -349825950.0 ? 77.0 : orderJ) ^ 3))
      }
          var roart: String! = String(cString: [109,112,106,112,101,103,0], encoding: .utf8)!
          var live9: String! = String(cString: [117,110,112,114,101,109,117,108,116,105,112,108,121,121,117,118,0], encoding: .utf8)!
          _ = live9
         orderJ /= Swift.max(1, Float(live9.count))
         roart.append("\(live9.count / 3)")
          var rushB: [Any]! = [85, 76, 0]
          var thumbnailM: [String: Any]! = [String(cString: [98,100,115,0], encoding: .utf8)!:String(cString: [97,118,103,115,97,100,0], encoding: .utf8)!]
          _ = thumbnailM
         detailsk += (Double(Int(detailsk > 91078579.0 || detailsk < -91078579.0 ? 41.0 : detailsk) ^ 1))
         rushB = [(1 * Int(orderJ > 176147368.0 || orderJ < -176147368.0 ? 5.0 : orderJ))]
         thumbnailM["\(rushB.count)"] = rushB.count << (Swift.min(labs(2), 4))
       var non3: String! = String(cString: [118,102,114,101,101,0], encoding: .utf8)!
         non3.append("\(non3.count)")
      edito.append((Int(orderJ > 270713963.0 || orderJ < -270713963.0 ? 8.0 : orderJ) % (Swift.max(v_titlej.count, 5))))

        if rateLimitTag == 311 { 
            MOHDiscover.gameLoadingSuccess("Report submitted, will be reviewed within 24 hours", in: self.view)
        }
        else { 
            MOHDiscover.overlayconfirm_alertpopup_interactionbounce(
                title: "Block User",
                message: "Are you sure you want to block this user? You will no longer see their content.",
                confirmTitle: "Block",
                in: self.view
            ) {
                FXBlazerunner.shared.throttlingburst_appenduser_toblacklist_spikesimulation(self.shortVideoData["uploadercreator_video_publishernickname_channel"] ?? "", avatar: self.shortVideoData["voiceovermicrophone_host_avatarimage_headset"] ?? "")
                self.navigationController?.popViewController(animated: true)
            }
        }
    }
}
