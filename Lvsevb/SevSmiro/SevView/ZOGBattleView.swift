
import Foundation

import UIKit

class ZOGBattleView: UIView {
private var local_4NewsRecord_string: String!
var proceedCoordinatordispatcherOp_string: String!
var dashEmberString: String?




    @IBOutlet weak var online_balance_label: UILabel! 
    @IBOutlet weak var online_number_label: UILabel! 
    @IBOutlet weak var collectionView: UICollectionView!
    @IBOutlet weak var online_reduct_button: UIButton! 
    @IBOutlet weak var online_add_button: UIButton! 
    var online_gifts_list: [[String: String]] = [[String: String]]()
    var online_select_tag: Int = 0
    var giftCount: Int = 1

@discardableResult
 func fatalInfinitySelectionSloganImmersiveScrollView(handlerMake: Float, iconFade: Bool) -> UIScrollView! {
    var accountsd: Double = 3.0
    var updatedI: [String: Any]! = [String(cString: [108,115,112,114,0], encoding: .utf8)!:String(cString: [111,117,116,112,111,105,110,116,0], encoding: .utf8)!, String(cString: [113,117,97,108,105,102,105,101,114,115,0], encoding: .utf8)!:String(cString: [102,108,97,116,110,101,115,115,0], encoding: .utf8)!, String(cString: [97,99,108,114,0], encoding: .utf8)!:String(cString: [99,114,111,112,0], encoding: .utf8)!]
   repeat {
       var willX: String! = String(cString: [116,104,114,101,97,100,110,97,109,101,0], encoding: .utf8)!
         willX.append("\(3)")
       var following4: Bool = true
       var messageK: Bool = true
         messageK = !messageK && following4
      accountsd /= Swift.max(2, (Double(Int(accountsd > 105451819.0 || accountsd < -105451819.0 ? 31.0 : accountsd))))
      if accountsd == 3061828.0 {
         break
      }
   } while (4 == (updatedI.count * 1) && (accountsd / (Swift.max(Double(updatedI.count), 8))) == 3.0) && (accountsd == 3061828.0)
   if updatedI["\(accountsd)"] == nil {
      updatedI = ["\(updatedI.keys.count)": (updatedI.count - Int(accountsd > 202371300.0 || accountsd < -202371300.0 ? 78.0 : accountsd))]
   }
     let attachNav: [Any]! = [3, 6]
     let completeLikes: Bool = false
     let strikeVortexknight: UILabel! = UILabel()
    var contrastBufferingIcwrsi:UIScrollView! = UIScrollView(frame:CGRect(x: 273, y: 445, width: 0, height: 0))
    contrastBufferingIcwrsi.frame = CGRect(x: 275, y: 310, width: 0, height: 0)
    contrastBufferingIcwrsi.alpha = 0.6;
    contrastBufferingIcwrsi.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    contrastBufferingIcwrsi.delegate = nil
    contrastBufferingIcwrsi.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    contrastBufferingIcwrsi.alwaysBounceVertical = true
    contrastBufferingIcwrsi.alwaysBounceHorizontal = true
    contrastBufferingIcwrsi.showsVerticalScrollIndicator = false
    contrastBufferingIcwrsi.showsHorizontalScrollIndicator = true
    strikeVortexknight.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    strikeVortexknight.alpha = 0.4
    strikeVortexknight.frame = CGRect(x: 88, y: 284, width: 0, height: 0)
    strikeVortexknight.font = UIFont.systemFont(ofSize:20)
    strikeVortexknight.text = ""
    strikeVortexknight.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    strikeVortexknight.textAlignment = .right
    
    contrastBufferingIcwrsi.addSubview(strikeVortexknight)

    
    return contrastBufferingIcwrsi

}





    
    
    
    @IBAction func onlineSendGiftsClick(_ sender: Any) {

         let spenderShareddownload: UIScrollView! = fatalInfinitySelectionSloganImmersiveScrollView(handlerMake:80.0, iconFade:false)

      if spenderShareddownload != nil {
          self.addSubview(spenderShareddownload)
          let spenderShareddownload_tag = spenderShareddownload.tag
      }
      else {
          print("spenderShareddownload is nil")      }

_ = spenderShareddownload


       var bossfightd: [String: Any]! = [String(cString: [105,100,101,110,116,105,116,105,101,115,0], encoding: .utf8)!:String(cString: [116,111,97,115,116,0], encoding: .utf8)!, String(cString: [114,101,109,97,112,112,101,100,0], encoding: .utf8)!:String(cString: [109,111,110,116,104,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &bossfightd) { pointer in
    
   }
    var selectedf: Double = 4.0
   withUnsafeMutablePointer(to: &selectedf) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 2 {
       var engagemente: [Any]! = [0]
       var alertp: Double = 5.0
      withUnsafeMutablePointer(to: &alertp) { pointer in
             _ = pointer.pointee
      }
         alertp -= (Double(Int(alertp > 69939433.0 || alertp < -69939433.0 ? 65.0 : alertp)))
          var stack9: Bool = true
          _ = stack9
          var contentG: Double = 2.0
          var progressm: Bool = false
         engagemente = [((progressm ? 5 : 3) % (Swift.max((stack9 ? 3 : 3), 6)))]
         contentG -= (Double(Int(contentG > 127027958.0 || contentG < -127027958.0 ? 54.0 : contentG) >> (Swift.min(3, labs((stack9 ? 3 : 3))))))
         alertp /= Swift.max(Double(1), 2)
      for _ in 0 ..< 3 {
         alertp /= Swift.max(Double(engagemente.count), 5)
      }
       var portraitN: Bool = true
      if 2.27 <= alertp || (2.27 / (Swift.max(10, alertp))) <= 3.15 {
         alertp -= Double(2 * engagemente.count)
      }
      bossfightd["\(selectedf)"] = (Int(selectedf > 292141752.0 || selectedf < -292141752.0 ? 65.0 : selectedf) - 2)
   }

        let delegate_x = online_gifts_list[online_select_tag]
        let proceed = Int(delegate_x["gifts_coins"] ?? "0") ?? 0
        let wallte = proceed * giftCount
        
        var btn = FXBlazerunner.shared.dashboardsnapshot_load_userprofile()
        let totalF = Int(btn["executor_wallet_coinsbalance_observerlistenerdelegate"] ?? "0") ?? 0
        
        if totalF < wallte {
            MOHDiscover.gameLoadingText("Insufficient coins, \(wallte) coins required.", in: self)
            return
        }
        
        btn["executor_wallet_coinsbalance_observerlistenerdelegate"] = "\(totalF - wallte)"
        FXBlazerunner.shared.dashboardsnapshot_save_userprofile(btn)
        refreshBalanceDisplay()
        
        MOHDiscover.gameLoadingSuccess("Sent \(giftCount) gift(s)! -\(wallte) coins", in: self)
        
        if let giftImage = UIImage(named: delegate_x["gifts_icon"] ?? "") {
            let btn = FXBlazerunner.shared.dashboardsnapshot_load_userprofile()
            let selectX = btn["processhandler_profile_nickname_manager"] ?? "Me"
            if let parentVC = self.superview {
                online_giftViewingAnimation(giftImage: giftImage, senderName: selectX, in: parentVC)
                if wallte >= 100 {
                    playFullScreenAnimation(giftImage: giftImage, senderName: selectX, in: parentVC)
                }
            }
        }
        
        giftCount = 1
        updateNumberLabel()
    }

@discardableResult
 func presentSignalTotalSceneNeverRechargeTableView(furyInfo: String!, validatorFanslist: String!, gradientThunder: Double) -> UITableView! {
    var canT: String! = String(cString: [99,111,110,118,0], encoding: .utf8)!
    var cell2: String! = String(cString: [102,105,108,101,112,97,116,104,0], encoding: .utf8)!
       var blitz_: [Any]! = [93, 73]
       var avatar6: [String: Any]! = [String(cString: [115,112,101,101,100,111,109,101,116,101,114,0], encoding: .utf8)!:16, String(cString: [113,115,118,115,99,97,108,101,0], encoding: .utf8)!:27]
      withUnsafeMutablePointer(to: &avatar6) { pointer in
    
      }
         blitz_ = [avatar6.count ^ blitz_.count]
          var teamupZ: String! = String(cString: [99,97,110,111,112,117,115,0], encoding: .utf8)!
          var generatorx: Bool = false
         avatar6[teamupZ] = 2
         generatorx = teamupZ == (String(cString:[97,0], encoding: .utf8)!)
          var self_3xQ: Bool = true
          _ = self_3xQ
         blitz_.append(blitz_.count)
          var codeu: String! = String(cString: [112,97,114,97,98,111,108,108,105,99,0], encoding: .utf8)!
         blitz_ = [avatar6.values.count]
         codeu = "\(avatar6.count)"
       var customV: Float = 3.0
       var expirationm: Float = 1.0
          var enters: Int = 2
         blitz_.append((Int(expirationm > 182245380.0 || expirationm < -182245380.0 ? 40.0 : expirationm) / (Swift.max(9, Int(customV > 314394592.0 || customV < -314394592.0 ? 16.0 : customV)))))
         enters -= avatar6.count
      canT.append("\(3 ^ avatar6.keys.count)")
      canT.append("\(canT.count)")
      cell2 = "\(((String(cString:[51,0], encoding: .utf8)!) == cell2 ? cell2.count : cell2.count))"
     let rushCode: UIButton! = UIButton()
     let targetRecords: UIImageView! = UIImageView(frame:CGRect.zero)
     var midnightBattle: Double = 70.0
     var buyStart: Float = 71.0
    var generatorLeading:UITableView! = UITableView()
    rushCode.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    rushCode.alpha = 1.0
    rushCode.frame = CGRect(x: 112, y: 67, width: 0, height: 0)
    rushCode.titleLabel?.font = UIFont.systemFont(ofSize:11)
    rushCode.setImage(UIImage(named:String(cString: [109,97,105,110,95,99,0], encoding: .utf8)!), for: .normal)
    rushCode.setTitle("", for: .normal)
    rushCode.setBackgroundImage(UIImage(named:String(cString: [114,101,115,116,111,114,101,100,0], encoding: .utf8)!), for: .normal)
    
    targetRecords.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    targetRecords.alpha = 0.3
    targetRecords.frame = CGRect(x: 33, y: 192, width: 0, height: 0)
    targetRecords.contentMode = .scaleAspectFit
    targetRecords.animationRepeatCount = 3
    targetRecords.image = UIImage(named:String(cString: [117,115,101,114,110,97,109,101,0], encoding: .utf8)!)
    
    generatorLeading.delegate = nil
    generatorLeading.dataSource = nil
    generatorLeading.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    generatorLeading.frame = CGRect(x: 166, y: 165, width: 0, height: 0)
    generatorLeading.alpha = 0.6;
    generatorLeading.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return generatorLeading

}





    
    func refreshBalanceDisplay() {

         let rftfsubDrop: UITableView! = presentSignalTotalSceneNeverRechargeTableView(furyInfo:String(cString: [99,111,108,115,112,97,110,0], encoding: .utf8)!, validatorFanslist:String(cString: [118,97,114,105,110,102,111,0], encoding: .utf8)!, gradientThunder:45.0)

      if rftfsubDrop != nil {
          let rftfsubDrop_tag = rftfsubDrop.tag
          self.addSubview(rftfsubDrop)
      }

_ = rftfsubDrop


       var sdbfjewbP: Float = 3.0
   withUnsafeMutablePointer(to: &sdbfjewbP) { pointer in
    
   }
       var immediatelyf: String! = String(cString: [112,97,114,97,109,0], encoding: .utf8)!
       var avatarsl: Double = 0.0
      repeat {
          var hjbewhjr: [Any]! = [30, 64]
          var willG: String! = String(cString: [115,116,117,98,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &willG) { pointer in
                _ = pointer.pointee
         }
          var bossfight9: [Any]! = [38, 61]
         withUnsafeMutablePointer(to: &bossfight9) { pointer in
                _ = pointer.pointee
         }
          var finish1: String! = String(cString: [105,116,101,114,97,116,105,111,110,0], encoding: .utf8)!
          _ = finish1
         avatarsl /= Swift.max(2, Double(immediatelyf.count))
         hjbewhjr = [(immediatelyf == (String(cString:[51,0], encoding: .utf8)!) ? immediatelyf.count : hjbewhjr.count)]
         willG = "\(hjbewhjr.count)"
         bossfight9.append(bossfight9.count)
         finish1.append("\(hjbewhjr.count)")
         if avatarsl == 1973854.0 {
            break
         }
      } while (avatarsl == 1973854.0) && (1 <= (1 * immediatelyf.count))
      repeat {
         immediatelyf = "\(3)"
         if (String(cString:[115,121,49,0], encoding: .utf8)!) == immediatelyf {
            break
         }
      } while ((String(cString:[115,121,49,0], encoding: .utf8)!) == immediatelyf) && (!immediatelyf.hasSuffix("\(avatarsl)"))
      if immediatelyf.hasSuffix("\(avatarsl)") {
         avatarsl -= Double(immediatelyf.count)
      }
         avatarsl -= (Double(Int(avatarsl > 288167253.0 || avatarsl < -288167253.0 ? 30.0 : avatarsl)))
          var verificatorU: String! = String(cString: [100,117,97,108,0], encoding: .utf8)!
          var introc: String! = String(cString: [97,116,114,97,99,97,108,0], encoding: .utf8)!
          var bigG: Int = 0
         avatarsl -= Double(verificatorU.count)
         introc.append("\(1)")
         bigG -= bigG + verificatorU.count
      if immediatelyf.count <= 4 {
         immediatelyf.append("\(1)")
      }
      sdbfjewbP -= Float(2)

        let btn = FXBlazerunner.shared.dashboardsnapshot_load_userprofile()
        let msg = btn["executor_wallet_coinsbalance_observerlistenerdelegate"] ?? "0"
        online_balance_label.text = msg
    }

@discardableResult
 func userGiftHandleInstanceSceneCompletely(dictTypes: [Any]!, verificatorCopy_7j: Double) -> String! {
    var successz: String! = String(cString: [114,105,102,102,0], encoding: .utf8)!
    var parentX: String! = String(cString: [98,105,111,109,101,116,114,105,99,115,0], encoding: .utf8)!
    var midnightU: String! = String(cString: [117,110,103,114,111,117,112,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &midnightU) { pointer in
    
   }
   while (!successz.contains(midnightU)) {
       var screenf: String! = String(cString: [114,101,97,99,104,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &screenf) { pointer in
             _ = pointer.pointee
      }
       var controllerL: Bool = false
       var black4: Int = 2
      withUnsafeMutablePointer(to: &black4) { pointer in
             _ = pointer.pointee
      }
       var resultf: [Any]! = [37, 73, 37]
       _ = resultf
       var nightX: String! = String(cString: [98,105,110,116,101,120,116,0], encoding: .utf8)!
         screenf.append("\(nightX.count >> (Swift.min(labs(1), 1)))")
         resultf = [2 + nightX.count]
      repeat {
          var cansv: String! = String(cString: [104,105,103,104,115,104,101,108,102,0], encoding: .utf8)!
          var frostbladeo: [String: Any]! = [String(cString: [110,118,99,0], encoding: .utf8)!:69, String(cString: [114,101,116,0], encoding: .utf8)!:3, String(cString: [115,117,98,116,101,120,116,0], encoding: .utf8)!:82]
          var warnemojiZ: Double = 3.0
         withUnsafeMutablePointer(to: &warnemojiZ) { pointer in
                _ = pointer.pointee
         }
          var and_jl6: String! = String(cString: [116,105,101,114,115,0], encoding: .utf8)!
         controllerL = 97 <= frostbladeo.values.count
         cansv = "\(3 >> (Swift.min(4, nightX.count)))"
         warnemojiZ -= Double(1 >> (Swift.min(2, labs(black4))))
         and_jl6 = "\(1 << (Swift.min(3, resultf.count)))"
         if controllerL ? !controllerL : controllerL {
            break
         }
      } while (controllerL ? !controllerL : controllerL) && (2 >= black4 || 3 >= (2 - black4))
          var fullR: String! = String(cString: [101,120,112,111,114,116,101,100,0], encoding: .utf8)!
          var key3: String! = String(cString: [101,120,101,99,117,116,97,98,108,101,0], encoding: .utf8)!
          _ = key3
          var itemsN: Float = 5.0
         black4 += (key3 == (String(cString:[53,0], encoding: .utf8)!) ? black4 : key3.count)
         fullR = "\(key3.count * screenf.count)"
         itemsN += (Float(key3 == (String(cString:[86,0], encoding: .utf8)!) ? key3.count : resultf.count))
      repeat {
          var safez: [String: Any]! = [String(cString: [115,116,97,116,115,0], encoding: .utf8)!:72, String(cString: [101,120,101,99,117,116,101,0], encoding: .utf8)!:4]
         nightX = "\(resultf.count)"
         safez[screenf] = screenf.count - 1
         if (String(cString:[100,113,112,53,111,0], encoding: .utf8)!) == nightX {
            break
         }
      } while ((String(cString:[100,113,112,53,111,0], encoding: .utf8)!) == nightX) && (nightX.count > 4)
         resultf = [screenf.count >> (Swift.min(labs(2), 1))]
          var strikeA: Int = 4
         withUnsafeMutablePointer(to: &strikeA) { pointer in
    
         }
          var orderz: [Any]! = [86, 5, 23]
         withUnsafeMutablePointer(to: &orderz) { pointer in
    
         }
         nightX = "\(resultf.count & 2)"
         strikeA /= Swift.max(orderz.count, 2)
         orderz.append(orderz.count)
       var f_manager5: String! = String(cString: [117,110,114,101,99,111,103,110,105,122,101,100,0], encoding: .utf8)!
       _ = f_manager5
          var dynamic_f7: String! = String(cString: [108,111,99,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
          _ = dynamic_f7
          var pausea: String! = String(cString: [110,97,108,117,0], encoding: .utf8)!
         black4 -= resultf.count
         dynamic_f7.append("\(((String(cString:[74,0], encoding: .utf8)!) == dynamic_f7 ? resultf.count : dynamic_f7.count))")
         pausea.append("\(((String(cString:[100,0], encoding: .utf8)!) == dynamic_f7 ? resultf.count : dynamic_f7.count))")
          var navH: Int = 0
          var keyf: Bool = false
         withUnsafeMutablePointer(to: &keyf) { pointer in
                _ = pointer.pointee
         }
          var generatorC: Bool = false
         nightX = "\((nightX == (String(cString:[103,0], encoding: .utf8)!) ? resultf.count : nightX.count))"
         navH &= (screenf.count % (Swift.max(9, (keyf ? 1 : 1))))
         keyf = (black4 ^ nightX.count) == 9
         generatorC = (String(cString:[109,0], encoding: .utf8)!) == screenf
          var fanslistu: [Any]! = [29, 33]
         resultf.append((resultf.count | (controllerL ? 3 : 5)))
         fanslistu = [nightX.count ^ 1]
         controllerL = 54 <= black4
         screenf.append("\((f_manager5 == (String(cString:[54,0], encoding: .utf8)!) ? black4 : f_manager5.count))")
         screenf = "\(black4)"
         resultf.append(black4)
      successz.append("\(screenf.count)")
      break
   }
       var delegate_yj: String! = String(cString: [114,117,115,115,105,97,110,0], encoding: .utf8)!
       _ = delegate_yj
      while (2 >= delegate_yj.count) {
          var filenameJ: Int = 0
         withUnsafeMutablePointer(to: &filenameJ) { pointer in
    
         }
          var commentsY: String! = String(cString: [112,111,105,110,116,99,98,98,0], encoding: .utf8)!
          var networkP: String! = String(cString: [111,112,97,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &networkP) { pointer in
                _ = pointer.pointee
         }
         delegate_yj = "\(commentsY.count)"
         filenameJ ^= delegate_yj.count
         networkP = "\(2)"
         break
      }
      for _ in 0 ..< 2 {
         delegate_yj.append("\(delegate_yj.count)")
      }
      while (delegate_yj.count <= delegate_yj.count) {
          var failedA: String! = String(cString: [102,114,111,110,116,0], encoding: .utf8)!
          var home2: Double = 2.0
         withUnsafeMutablePointer(to: &home2) { pointer in
                _ = pointer.pointee
         }
          var hjbewhjC: String! = String(cString: [115,108,111,119,100,111,119,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &hjbewhjC) { pointer in
                _ = pointer.pointee
         }
         delegate_yj.append("\(hjbewhjC.count >> (Swift.min(labs(1), 1)))")
         failedA = "\(1)"
         home2 += Double(failedA.count)
         break
      }
      successz.append("\(parentX.count / (Swift.max(9, successz.count)))")
       var picker7: String! = String(cString: [100,101,98,117,103,103,101,114,0], encoding: .utf8)!
      repeat {
         picker7 = "\(picker7.count + picker7.count)"
         if picker7.count == 2710667 {
            break
         }
      } while (picker7.count >= picker7.count) && (picker7.count == 2710667)
      for _ in 0 ..< 2 {
         picker7.append("\(1 | picker7.count)")
      }
          var descB: Float = 3.0
         withUnsafeMutablePointer(to: &descB) { pointer in
                _ = pointer.pointee
         }
         picker7 = "\((picker7 == (String(cString:[86,0], encoding: .utf8)!) ? picker7.count : Int(descB > 350179571.0 || descB < -350179571.0 ? 67.0 : descB)))"
      parentX.append("\(parentX.count >> (Swift.min(successz.count, 2)))")
   return successz

}





    
    func online_giftsMetedataList() {

         let metadatasContainable: String! = userGiftHandleInstanceSceneCompletely(dictTypes:[18, 14, 5], verificatorCopy_7j:45.0)

      print(metadatasContainable)
      let metadatasContainable_len = metadatasContainable?.count ?? 0

_ = metadatasContainable


       var none: String! = String(cString: [100,101,102,97,117,108,116,115,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      none = "\((none == (String(cString:[66,0], encoding: .utf8)!) ? none.count : none.count))"
   }

        online_gifts_list = [
            ["gifts_icon": "nightechoMorePurple", "gifts_coins": "9"],
            ["gifts_icon": "likeCollection", "gifts_coins": "19"],
            ["gifts_icon": "rainbowPixel", "gifts_coins": "29"],
            ["gifts_icon": "likeNewsCombo", "gifts_coins": "39"],
            ["gifts_icon": "transactionsSdfnjksdfMontage", "gifts_coins": "49"],
            ["gifts_icon": "duskwardenCircle", "gifts_coins": "59"],
            ["gifts_icon": "postPurpleHighlights", "gifts_coins": "69"],
            ["gifts_icon": "wejkbndsfApple", "gifts_coins": "79"],
        ]
    }

@discardableResult
 func networkCountNightShowEffect(passwordSelected: Bool, expirationCollection: String!) -> Bool {
    var componentsI: [String: Any]! = [String(cString: [102,101,97,116,117,114,101,115,0], encoding: .utf8)!:59, String(cString: [99,111,109,112,97,114,101,0], encoding: .utf8)!:42]
    var likeS: Int = 4
   withUnsafeMutablePointer(to: &likeS) { pointer in
    
   }
    var neond: Bool = false
   for _ in 0 ..< 3 {
       var crystalc: String! = String(cString: [112,97,103,105,110,97,116,105,111,110,0], encoding: .utf8)!
       _ = crystalc
       var noteK: Int = 5
       var short_pv: String! = String(cString: [111,112,116,105,109,117,109,0], encoding: .utf8)!
       var highlights2: String! = String(cString: [115,111,102,97,0], encoding: .utf8)!
       var playf: String! = String(cString: [110,97,118,105,103,97,116,105,111,110,0], encoding: .utf8)!
       _ = playf
         highlights2.append("\(1 & highlights2.count)")
      if playf != short_pv {
         short_pv.append("\(((String(cString:[79,0], encoding: .utf8)!) == highlights2 ? highlights2.count : crystalc.count))")
      }
      if !highlights2.contains("\(playf.count)") {
         playf.append("\(highlights2.count)")
      }
         crystalc.append("\(short_pv.count)")
         highlights2.append("\(crystalc.count | highlights2.count)")
          var presentationQ: Double = 4.0
          var rendererJ: String! = String(cString: [97,100,100,0], encoding: .utf8)!
          var ltexts: [String: Any]! = [String(cString: [116,114,116,97,98,108,101,0], encoding: .utf8)!:39, String(cString: [116,101,109,112,115,0], encoding: .utf8)!:9, String(cString: [115,108,105,100,101,115,104,111,119,0], encoding: .utf8)!:38]
         withUnsafeMutablePointer(to: &ltexts) { pointer in
                _ = pointer.pointee
         }
         highlights2.append("\(short_pv.count)")
         presentationQ -= Double(crystalc.count)
         rendererJ = "\(crystalc.count & 3)"
         ltexts[short_pv] = 1
      repeat {
         crystalc = "\((short_pv == (String(cString:[109,0], encoding: .utf8)!) ? short_pv.count : highlights2.count))"
         if crystalc.count == 951219 {
            break
         }
      } while (crystalc.count == 951219) && (crystalc.contains("\(noteK)"))
      while ((noteK % (Swift.max(playf.count, 2))) > 3 && (noteK % (Swift.max(playf.count, 2))) > 3) {
         noteK ^= noteK
         break
      }
      repeat {
         highlights2.append("\(short_pv.count)")
         if 1006908 == highlights2.count {
            break
         }
      } while (1006908 == highlights2.count) && (playf.count < highlights2.count)
          var people4: Double = 0.0
         highlights2 = "\(highlights2.count)"
         people4 /= Swift.max((Double(playf == (String(cString:[100,0], encoding: .utf8)!) ? highlights2.count : playf.count)), 4)
         noteK %= Swift.max(2, playf.count)
      repeat {
         short_pv.append("\(3)")
         if short_pv.count == 2058323 {
            break
         }
      } while (short_pv.hasSuffix("\(noteK)")) && (short_pv.count == 2058323)
         short_pv.append("\(3 >> (Swift.min(4, playf.count)))")
         playf.append("\(playf.count * 2)")
         playf.append("\(((String(cString:[88,0], encoding: .utf8)!) == playf ? playf.count : noteK))")
      componentsI["\(noteK)"] = componentsI.values.count
   }
   for _ in 0 ..< 1 {
      neond = likeS <= 19
   }
   return neond

}





    
    @IBAction func onlineDismissGiftsViewClick(_ sender: Any) {

         let queueScte: Bool = networkCountNightShowEffect(passwordSelected:true, expirationCollection:String(cString: [112,114,101,116,101,110,100,0], encoding: .utf8)!)

      if !queueScte {
          print("interest")
      }

_ = queueScte


       var g_managerW: Double = 2.0
      g_managerW += (Double(Int(g_managerW > 235452340.0 || g_managerW < -235452340.0 ? 16.0 : g_managerW)))

        UIView.animate(withDuration: 0.29) {
            self.frame = CGRect(x: 0, y: 1200, width: UIScreen.main.bounds.width, height: UIScreen.main.bounds.height)
        }
    }

@discardableResult
 func initialConnectionLoadButton(storePost: String!, emptyTabbar: [Any]!, raidChat: [Any]!) -> UIButton! {
    var containery: String! = String(cString: [98,105,116,101,0], encoding: .utf8)!
    var descW: String! = String(cString: [116,101,120,116,108,101,0], encoding: .utf8)!
   repeat {
       var clash0: [Any]! = [99, 46, 19]
       var recentZ: Double = 2.0
       _ = recentZ
       var actionY: Double = 3.0
      withUnsafeMutablePointer(to: &actionY) { pointer in
    
      }
       var skydriftert: String! = String(cString: [101,120,112,108,105,99,105,116,0], encoding: .utf8)!
       var displayn: Float = 1.0
      withUnsafeMutablePointer(to: &displayn) { pointer in
    
      }
          var groupQ: String! = String(cString: [98,114,97,110,100,115,0], encoding: .utf8)!
         clash0.append((Int(recentZ > 110403658.0 || recentZ < -110403658.0 ? 19.0 : recentZ)))
         groupQ = "\((Int(recentZ > 237913137.0 || recentZ < -237913137.0 ? 52.0 : recentZ) % (Swift.max(1, Int(actionY > 349339162.0 || actionY < -349339162.0 ? 83.0 : actionY)))))"
         recentZ /= Swift.max(Double(2), 5)
      while (5 <= (3 * skydriftert.count)) {
         actionY -= (Double(skydriftert.count & Int(displayn > 177240978.0 || displayn < -177240978.0 ? 83.0 : displayn)))
         break
      }
      repeat {
         recentZ -= (Double(skydriftert.count * Int(actionY > 242775887.0 || actionY < -242775887.0 ? 64.0 : actionY)))
         if 1418967.0 == recentZ {
            break
         }
      } while (3.14 > (Double(actionY * Double(2)))) && (1418967.0 == recentZ)
      while (!skydriftert.hasPrefix("\(displayn)")) {
          var fanslist_: Float = 1.0
          var gameplayi: String! = String(cString: [115,117,103,103,101,115,116,105,111,110,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &gameplayi) { pointer in
                _ = pointer.pointee
         }
          var fieldW: String! = String(cString: [97,112,112,108,105,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &fieldW) { pointer in
    
         }
         skydriftert = "\((Int(actionY > 35405028.0 || actionY < -35405028.0 ? 55.0 : actionY) / 3))"
         fanslist_ -= Float(gameplayi.count + fieldW.count)
         gameplayi = "\(fieldW.count & 1)"
         break
      }
         clash0.append(skydriftert.count)
      while (skydriftert.hasPrefix("\(actionY)")) {
         actionY += (Double(Int(recentZ > 85728443.0 || recentZ < -85728443.0 ? 29.0 : recentZ)))
         break
      }
         clash0.append(skydriftert.count << (Swift.min(labs(2), 5)))
         actionY -= (Double(Int(displayn > 310851222.0 || displayn < -310851222.0 ? 42.0 : displayn) ^ Int(actionY > 207376670.0 || actionY < -207376670.0 ? 57.0 : actionY)))
      repeat {
          var navh: String! = String(cString: [115,98,99,100,115,112,0], encoding: .utf8)!
          var selectg: Bool = false
          var closeG: String! = String(cString: [117,110,100,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &closeG) { pointer in
                _ = pointer.pointee
         }
          var dictd: String! = String(cString: [112,111,115,116,105,110,105,116,0], encoding: .utf8)!
          var strike3: String! = String(cString: [116,104,114,109,97,116,0], encoding: .utf8)!
         clash0 = [3 ^ closeG.count]
         navh.append("\(3)")
         selectg = clash0.count <= 86 && recentZ <= 59.79
         dictd.append("\(2 & closeG.count)")
         strike3 = "\(((selectg ? 2 : 3)))"
         if clash0.count == 1332381 {
            break
         }
      } while (2.41 >= (2.22 * actionY) || 4.66 >= (actionY * 2.22)) && (clash0.count == 1332381)
       var midnightL: Int = 2
       var resultb: Int = 3
         resultb -= (1 + Int(recentZ > 240801736.0 || recentZ < -240801736.0 ? 91.0 : recentZ))
         displayn -= (Float(Int(actionY > 156128324.0 || actionY < -156128324.0 ? 49.0 : actionY)))
      for _ in 0 ..< 2 {
         actionY -= (Double(Int(actionY > 133187979.0 || actionY < -133187979.0 ? 85.0 : actionY)))
      }
         actionY += (Double(2 - Int(actionY > 363669060.0 || actionY < -363669060.0 ? 31.0 : actionY)))
         midnightL %= Swift.max(3, skydriftert.count)
      containery = "\(clash0.count)"
      if containery == (String(cString:[116,51,106,48,104,107,118,48,0], encoding: .utf8)!) {
         break
      }
   } while (containery == (String(cString:[116,51,106,48,104,107,118,48,0], encoding: .utf8)!)) && (4 == containery.count)
   repeat {
       var publisherj: Float = 3.0
       var comment2: String! = String(cString: [103,114,97,121,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &comment2) { pointer in
             _ = pointer.pointee
      }
       var cbdfj: String! = String(cString: [106,101,114,114,111,114,0], encoding: .utf8)!
       var resumee: Double = 5.0
      withUnsafeMutablePointer(to: &resumee) { pointer in
    
      }
       var buyY: Double = 2.0
      while ((Int(resumee > 54510633.0 || resumee < -54510633.0 ? 54.0 : resumee) + 1) >= 1 && (5.61 + resumee) >= 3.88) {
          var sdjfndhsF: String! = String(cString: [114,101,112,108,97,99,101,115,0], encoding: .utf8)!
          var altp: String! = String(cString: [115,111,117,114,99,101,0], encoding: .utf8)!
          var itemsE: Bool = true
         withUnsafeMutablePointer(to: &itemsE) { pointer in
    
         }
          var ynewsi: Bool = false
          var followingA: Double = 4.0
         withUnsafeMutablePointer(to: &followingA) { pointer in
    
         }
         resumee += Double(cbdfj.count)
         sdjfndhsF = "\((Int(followingA > 230376459.0 || followingA < -230376459.0 ? 83.0 : followingA)))"
         altp.append("\((sdjfndhsF == (String(cString:[112,0], encoding: .utf8)!) ? sdjfndhsF.count : (ynewsi ? 5 : 5)))")
         itemsE = comment2.count < sdjfndhsF.count
         followingA -= (Double((itemsE ? 3 : 4)))
         break
      }
          var crimsonarkc: Double = 3.0
         cbdfj.append("\(comment2.count)")
         crimsonarkc -= (Double(Int(buyY > 29091450.0 || buyY < -29091450.0 ? 56.0 : buyY)))
          var stackC: Bool = true
         withUnsafeMutablePointer(to: &stackC) { pointer in
    
         }
          var bindA: String! = String(cString: [101,120,112,105,114,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &bindA) { pointer in
    
         }
          var invalidr: String! = String(cString: [100,101,101,112,0], encoding: .utf8)!
         comment2 = "\(((stackC ? 2 : 1)))"
         bindA = "\(1)"
         invalidr.append("\(cbdfj.count | 1)")
      if cbdfj.count > (Int(resumee > 68264883.0 || resumee < -68264883.0 ? 56.0 : resumee)) {
         cbdfj = "\(((String(cString:[56,0], encoding: .utf8)!) == comment2 ? Int(publisherj > 328133902.0 || publisherj < -328133902.0 ? 22.0 : publisherj) : comment2.count))"
      }
         cbdfj.append("\(cbdfj.count / 1)")
         resumee += (Double(cbdfj == (String(cString:[121,0], encoding: .utf8)!) ? Int(resumee > 291789191.0 || resumee < -291789191.0 ? 89.0 : resumee) : cbdfj.count))
          var tabbaro: Double = 3.0
          _ = tabbaro
         resumee -= Double(3)
         tabbaro -= (Double(Int(buyY > 320411506.0 || buyY < -320411506.0 ? 16.0 : buyY) * 2))
          var followed3: String! = String(cString: [99,104,97,110,103,101,100,0], encoding: .utf8)!
          var emberl: Int = 0
         withUnsafeMutablePointer(to: &emberl) { pointer in
    
         }
         cbdfj.append("\(2)")
         followed3.append("\(cbdfj.count ^ followed3.count)")
         emberl >>= Swift.min(5, labs(followed3.count | emberl))
         comment2 = "\(1)"
       var type_y4: [Any]! = [true]
       var cancelledj: [Any]! = [String(cString: [114,101,99,108,97,105,109,0], encoding: .utf8)!, String(cString: [112,114,101,115,101,116,0], encoding: .utf8)!]
         cbdfj = "\((Int(publisherj > 52003925.0 || publisherj < -52003925.0 ? 58.0 : publisherj)))"
      for _ in 0 ..< 3 {
         publisherj -= (Float(Int(buyY > 352909204.0 || buyY < -352909204.0 ? 58.0 : buyY) % 2))
      }
      while ((cancelledj.count | cbdfj.count) > 2 || 5 > (cancelledj.count | 2)) {
         cancelledj = [1]
         break
      }
          var and_mG: Bool = false
         cbdfj = "\(((and_mG ? 1 : 3)))"
       var h_center1: [Any]! = [13.0]
         type_y4 = [h_center1.count | 3]
         h_center1.append(3)
      containery = "\(((String(cString:[119,0], encoding: .utf8)!) == comment2 ? comment2.count : Int(resumee > 350131742.0 || resumee < -350131742.0 ? 100.0 : resumee)))"
      if containery.count == 1883259 {
         break
      }
   } while (containery.count > descW.count) && (containery.count == 1883259)
     var scrollInteraction: UIImageView! = UIImageView(frame:CGRect.zero)
     var purchaseFriend_3: String! = String(cString: [115,104,105,112,112,105,110,103,0], encoding: .utf8)!
     let enterScreen: Bool = false
     var blitzAsk: String! = String(cString: [99,111,102,97,99,116,111,114,0], encoding: .utf8)!
    var comfortXlabelwidth = UIButton()
    scrollInteraction.frame = CGRect(x: 80, y: 197, width: 0, height: 0)
    scrollInteraction.alpha = 0.5;
    scrollInteraction.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    scrollInteraction.image = UIImage(named:String(cString: [100,95,112,114,111,100,117,99,116,115,0], encoding: .utf8)!)
    scrollInteraction.contentMode = .scaleAspectFit
    scrollInteraction.animationRepeatCount = 6
    
    comfortXlabelwidth.setBackgroundImage(UIImage(named:String(cString: [106,115,111,110,0], encoding: .utf8)!), for: .normal)
    comfortXlabelwidth.titleLabel?.font = UIFont.systemFont(ofSize:13)
    comfortXlabelwidth.setImage(UIImage(named:String(cString: [101,109,98,101,114,0], encoding: .utf8)!), for: .normal)
    comfortXlabelwidth.setTitle("", for: .normal)
    comfortXlabelwidth.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    comfortXlabelwidth.alpha = 0.1
    comfortXlabelwidth.frame = CGRect(x: 55, y: 83, width: 0, height: 0)

    
    return comfortXlabelwidth

}





    
    override func awakeFromNib() {

         var beingDecoder: UIButton! = initialConnectionLoadButton(storePost:String(cString: [115,108,105,100,0], encoding: .utf8)!, emptyTabbar:[[64.0]], raidChat:[String(cString: [117,110,101,115,99,97,112,105,110,103,0], encoding: .utf8)!, String(cString: [116,105,110,116,101,100,0], encoding: .utf8)!])

      if beingDecoder != nil {
          let beingDecoder_tag = beingDecoder.tag
          self.addSubview(beingDecoder)
      }
      else {
          print("beingDecoder is nil")      }

withUnsafeMutablePointer(to: &beingDecoder) { pointer in
    
}


       var subtitleg: [String: Any]! = [String(cString: [112,115,101,117,100,111,0], encoding: .utf8)!:[UILabel()]]
   withUnsafeMutablePointer(to: &subtitleg) { pointer in
    
   }
       var localizedC: Int = 3
       var broadcastO: Double = 3.0
       var group5: Double = 2.0
      withUnsafeMutablePointer(to: &group5) { pointer in
             _ = pointer.pointee
      }
       var coordinatordispatcher4: Double = 2.0
      if 3.83 >= (coordinatordispatcher4 - group5) && (3.83 / (Swift.max(6, coordinatordispatcher4))) >= 2.71 {
         coordinatordispatcher4 /= Swift.max(Double(1), 5)
      }
          var battleW: [String: Any]! = [String(cString: [110,111,110,98,108,111,99,107,0], encoding: .utf8)!:44, String(cString: [99,108,97,122,122,0], encoding: .utf8)!:34, String(cString: [109,116,105,109,101,0], encoding: .utf8)!:35]
         localizedC -= localizedC | 2
         battleW = ["\(localizedC)": (Int(broadcastO > 351803911.0 || broadcastO < -351803911.0 ? 68.0 : broadcastO))]
      while ((2.56 - coordinatordispatcher4) <= 5.35 || (coordinatordispatcher4 - 2.56) <= 5.48) {
         broadcastO += Double(1)
         break
      }
      for _ in 0 ..< 1 {
         localizedC ^= (Int(broadcastO > 293620571.0 || broadcastO < -293620571.0 ? 97.0 : broadcastO))
      }
         broadcastO /= Swift.max(5, (Double(1 & Int(broadcastO > 109058280.0 || broadcastO < -109058280.0 ? 37.0 : broadcastO))))
      subtitleg = ["\(subtitleg.values.count)": (Int(broadcastO > 128808103.0 || broadcastO < -128808103.0 ? 17.0 : broadcastO))]

        super.awakeFromNib()
        online_giftsMetedataList()
        setupUIOnlineGiftsCollectionView()
        refreshBalanceDisplay()
        updateNumberLabel()
    }

    
    @IBAction func onlineAddOrShaoCountClick(_ sender: UIButton) {
       var window_8yS: [Any]! = [98, 87, 34]
   if (3 | window_8yS.count) >= 3 {
      window_8yS = [2 & window_8yS.count]
   }

        if sender.tag == 311 { 
            if giftCount > 1 {
                giftCount -= 1
            }
        }
        else { 
            giftCount += 1
        }
        updateNumberLabel()
    }

    
    func updateNumberLabel() {
       var and_hoV: String! = String(cString: [116,114,105,0], encoding: .utf8)!
    var effectC: String! = String(cString: [112,97,116,104,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &effectC) { pointer in
    
   }
      effectC.append("\(((String(cString:[74,0], encoding: .utf8)!) == effectC ? and_hoV.count : effectC.count))")

      and_hoV.append("\(1)")
        let delegate_x = online_gifts_list[online_select_tag]
        let proceed = Int(delegate_x["gifts_coins"] ?? "0") ?? 0
        let zone = proceed * giftCount
        online_number_label.text = "\(giftCount)"
    }

    
    func playFullScreenAnimation(giftImage: UIImage, senderName: String, in containerView: UIView) {
       var sdjfndhsm: Double = 1.0
    _ = sdjfndhsm
   if sdjfndhsm <= 2.3 {
       var targetM: String! = String(cString: [114,101,108,97,116,105,118,101,108,121,0], encoding: .utf8)!
       var communityK: Float = 0.0
       var failedO: Int = 3
       var reliable5: [Any]! = [10, 19]
       _ = reliable5
         communityK /= Swift.max(2, (Float(Int(communityK > 15360283.0 || communityK < -15360283.0 ? 27.0 : communityK) % (Swift.max(reliable5.count, 6)))))
          var pickerm: String! = String(cString: [105,116,101,114,97,116,105,118,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &pickerm) { pointer in
    
         }
          var giftsP: String! = String(cString: [122,101,116,97,0], encoding: .utf8)!
         communityK /= Swift.max((Float((String(cString:[102,0], encoding: .utf8)!) == targetM ? targetM.count : reliable5.count)), 4)
         pickerm.append("\((Int(communityK > 281600713.0 || communityK < -281600713.0 ? 63.0 : communityK)))")
         giftsP.append("\(targetM.count)")
         communityK /= Swift.max(5, Float(reliable5.count))
      for _ in 0 ..< 2 {
          var raidQ: String! = String(cString: [116,120,116,0], encoding: .utf8)!
          var dictk: Double = 3.0
          _ = dictk
          var linef: Double = 3.0
         withUnsafeMutablePointer(to: &linef) { pointer in
                _ = pointer.pointee
         }
         failedO &= (Int(linef > 243793993.0 || linef < -243793993.0 ? 13.0 : linef))
         raidQ = "\((1 | Int(linef > 304476281.0 || linef < -304476281.0 ? 31.0 : linef)))"
         dictk /= Swift.max(2, Double(2))
      }
      while (5 == reliable5.count) {
         reliable5 = [failedO]
         break
      }
         communityK /= Swift.max(Float(2), 2)
          var titanR: String! = String(cString: [111,114,105,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &titanR) { pointer in
    
         }
          var finishv: Float = 0.0
          var epicG: String! = String(cString: [97,99,99,101,112,116,0], encoding: .utf8)!
         communityK -= Float(2 | reliable5.count)
         titanR = "\(targetM.count | epicG.count)"
         finishv /= Swift.max(Float(targetM.count ^ 1), 1)
         epicG.append("\(reliable5.count - 2)")
       var preview_: String! = String(cString: [101,100,105,116,105,110,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &preview_) { pointer in
             _ = pointer.pointee
      }
       var pravicyH: String! = String(cString: [102,100,111,112,101,110,0], encoding: .utf8)!
       _ = pravicyH
      if (pravicyH.count ^ 3) <= 2 && (pravicyH.count ^ 3) <= 4 {
         pravicyH.append("\(reliable5.count)")
      }
       var duskwardenS: String! = String(cString: [111,117,116,102,105,108,101,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
         preview_ = "\(duskwardenS.count & pravicyH.count)"
      }
      while ((reliable5.count + failedO) > 3) {
          var discount7: String! = String(cString: [102,105,110,97,108,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &discount7) { pointer in
    
         }
         failedO ^= (2 << (Swift.min(3, labs(Int(communityK > 171257472.0 || communityK < -171257472.0 ? 7.0 : communityK)))))
         discount7 = "\(2 - targetM.count)"
         break
      }
      sdjfndhsm -= Double(targetM.count)
   }

    
        let giftY = UIView(frame: containerView.bounds)
        giftY.backgroundColor = UIColor.white.withAlphaComponent(0.6)
        giftY.alpha = 0
        containerView.addSubview(giftY)
        UIView.animateKeyframes(withDuration: 0.6, delay: 0) {
            UIView.addKeyframe(withRelativeStartTime: 0, relativeDuration: 0.15) { giftY.alpha = 1 }
            UIView.addKeyframe(withRelativeStartTime: 0.15, relativeDuration: 0.85) { giftY.alpha = 0 }
        } completion: { _ in giftY.removeFromSuperview() }
        
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.2) {
            let blur = CALayer()
            blur.contents = giftImage.cgImage
            blur.contentsGravity = .resizeAspect
            blur.bounds = CGRect(x: 0, y: 0, width: 200, height: 200)
            blur.position = CGPoint(x: containerView.bounds.midX, y: containerView.bounds.midY - 50)
            blur.opacity = 0
            containerView.layer.addSublayer(blur)
            
            
            let alt = CASpringAnimation(keyPath: "transform.scale")
            alt.fromValue = 0
            alt.toValue = 1.0
            alt.stiffness = 150
            alt.damping = 12
            alt.mass = 2.0
            alt.initialVelocity = 10
            alt.duration = alt.settlingDuration
            
            let can = CABasicAnimation(keyPath: "opacity")
            can.fromValue = 0; can.toValue = 1; can.duration = 0.15
            
            let accounts = CAAnimationGroup()
            accounts.animations = [alt, can]
            accounts.duration = alt.settlingDuration
            accounts.fillMode = .forwards
            accounts.isRemovedOnCompletion = false
            blur.add(accounts, forKey: "burst")
            
            
            let register_3 = CAShapeLayer()
            register_3.path = UIBezierPath(arcCenter: blur.position, radius: 40, startAngle: 0, endAngle: .pi * 2, clockwise: true).cgPath
            register_3.fillColor = UIColor.clear.cgColor
            register_3.strokeColor = UIColor(red: 1, green: 0.84, blue: 0, alpha: 0.8).cgColor
            register_3.lineWidth = 3; register_3.opacity = 0
            containerView.layer.addSublayer(register_3)
            
            let combo = CABasicAnimation(keyPath: "transform.scale")
            combo.fromValue = 1; combo.toValue = 4; combo.duration = 0.8
            let apple = CABasicAnimation(keyPath: "opacity")
            apple.fromValue = 1; apple.toValue = 0; apple.duration = 0.8
            let k_title = CAAnimationGroup()
            k_title.animations = [combo, apple]; k_title.duration = 0.8
            k_title.fillMode = .forwards; k_title.isRemovedOnCompletion = false
            CATransaction.begin()
            CATransaction.setCompletionBlock { register_3.removeFromSuperlayer() }
            register_3.add(k_title, forKey: "glow")
            CATransaction.commit()
            
            
            DispatchQueue.main.asyncAfter(deadline: .now() + 1.2) {
                let with_2a = CABasicAnimation(keyPath: "opacity")
                with_2a.fromValue = 1; with_2a.toValue = 0; with_2a.duration = 0.4
                with_2a.fillMode = .forwards; with_2a.isRemovedOnCompletion = false
                CATransaction.begin()
                CATransaction.setCompletionBlock { blur.removeFromSuperlayer() }
                blur.add(with_2a, forKey: "fadeOut")
                CATransaction.commit()
            }
        }
        
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            let valid = CAEmitterLayer()
            valid.emitterPosition = CGPoint(x: containerView.bounds.midX, y: -20)
            valid.emitterSize = CGSize(width: containerView.bounds.width, height: 1)
            valid.emitterShape = .line
            
            let performCell = CAEmitterCell()
            performCell.contents = giftImage.cgImage
            performCell.birthRate = 12
            performCell.lifetime = 5
            performCell.velocity = 180
            performCell.velocityRange = 60
            performCell.yAcceleration = 50
            performCell.spin = 1.5
            performCell.spinRange = 3
            performCell.scale = 0.15
            performCell.scaleRange = 0.08
            performCell.alphaSpeed = -0.12
            performCell.emissionLongitude = .pi
            performCell.emissionRange = .pi / 4
            
            valid.emitterCells = [performCell]
            containerView.layer.addSublayer(valid)
            
            
            DispatchQueue.main.asyncAfter(deadline: .now() + 3) {
                valid.birthRate = 0
                DispatchQueue.main.asyncAfter(deadline: .now() + 5) {
                    valid.removeFromSuperlayer()
                }
            }
        }
    }

@discardableResult
 func collectionRawUserFinishAnchor(and_p6Gameplay: String!) -> String! {
    var handshakeg: Bool = true
    var flashO: [String: Any]! = [String(cString: [99,114,117,110,0], encoding: .utf8)!:UILabel()]
    var altG: String! = String(cString: [104,121,115,99,97,108,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &altG) { pointer in
          _ = pointer.pointee
   }
       var a_titleV: String! = String(cString: [104,121,115,116,101,114,101,115,105,115,0], encoding: .utf8)!
         a_titleV = "\(2)"
      while (2 < a_titleV.count) {
         a_titleV.append("\(a_titleV.count % (Swift.max(2, 4)))")
         break
      }
         a_titleV.append("\(a_titleV.count + a_titleV.count)")
      altG = "\((a_titleV == (String(cString:[68,0], encoding: .utf8)!) ? a_titleV.count : (handshakeg ? 1 : 5)))"
      flashO = ["\(flashO.count)": altG.count]
   return altG

}





    
    func setupUIOnlineGiftsCollectionView() {

         var postfixWindow: String! = collectionRawUserFinishAnchor(and_p6Gameplay:String(cString: [98,108,117,114,114,101,100,0], encoding: .utf8)!)

      print(postfixWindow)
      let postfixWindow_len = postfixWindow?.count ?? 0

withUnsafeMutablePointer(to: &postfixWindow) { pointer in
    
}


       var swifty0: [Any]! = [String(cString: [100,101,115,116,105,110,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [100,101,99,111,114,114,0], encoding: .utf8)!, String(cString: [102,117,110,110,121,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &swifty0) { pointer in
          _ = pointer.pointee
   }
   while (!swifty0.contains { $0 as? Int == swifty0.count }) {
       var agreemento: String! = String(cString: [108,114,111,110,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &agreemento) { pointer in
             _ = pointer.pointee
      }
         agreemento.append("\(((String(cString:[114,0], encoding: .utf8)!) == agreemento ? agreemento.count : agreemento.count))")
      while (agreemento.contains("\(agreemento.count)")) {
         agreemento = "\(2)"
         break
      }
      if 2 >= agreemento.count {
         agreemento = "\(1 / (Swift.max(5, agreemento.count)))"
      }
      swifty0 = [swifty0.count | agreemento.count]
      break
   }

        let portrait = UICollectionViewFlowLayout()
        portrait.scrollDirection = .vertical
        portrait.minimumInteritemSpacing = 15
        portrait.minimumLineSpacing = 12
        portrait.itemSize = CGSize(width: (UIScreen.main.bounds.width - 85.1)/4, height: 104)
        portrait.sectionInset = UIEdgeInsets(top: 0, left: 20, bottom: 0, right: 20)
        
        collectionView.collectionViewLayout = portrait
        collectionView.backgroundColor = .clear
        collectionView.dataSource = self
        collectionView.delegate = self
        collectionView.register(UINib(nibName: "QLaunchCell", bundle: nil), forCellWithReuseIdentifier: "gifts")
    }

    
}

extension ZOGBattleView: UICollectionViewDataSource, UICollectionViewDelegate {

@discardableResult
 func unsafeInterestOriginError(ltextBlack: Int) -> String! {
    var dismissd: Int = 1
    var loadingO: String! = String(cString: [114,101,115,101,114,118,101,0], encoding: .utf8)!
    _ = loadingO
    var rawH: String! = String(cString: [99,108,97,117,115,101,115,0], encoding: .utf8)!
       var moreK: String! = String(cString: [99,98,115,110,105,100,0], encoding: .utf8)!
       _ = moreK
       var optionsA: Double = 0.0
         optionsA /= Swift.max(Double(1), 4)
       var dashG: [Any]! = [2, 16]
      repeat {
         optionsA /= Swift.max((Double(Int(optionsA > 36386839.0 || optionsA < -36386839.0 ? 44.0 : optionsA))), 3)
         if optionsA == 567592.0 {
            break
         }
      } while (optionsA == 567592.0) && ((dashG.count << (Swift.min(labs(1), 5))) < 4)
      for _ in 0 ..< 3 {
         dashG = [(moreK.count >> (Swift.min(4, labs(Int(optionsA > 187805147.0 || optionsA < -187805147.0 ? 82.0 : optionsA)))))]
      }
      repeat {
         dashG = [(2 * Int(optionsA > 179944385.0 || optionsA < -179944385.0 ? 6.0 : optionsA))]
         if dashG.count == 80113 {
            break
         }
      } while ((optionsA / 2.50) > 5.64 || 1.17 > (2.50 / (Swift.max(8, optionsA)))) && (dashG.count == 80113)
      while (!moreK.hasPrefix("\(dashG.count)")) {
         dashG = [3 * dashG.count]
         break
      }
      rawH = "\(rawH.count)"
      rawH.append("\(((String(cString:[116,0], encoding: .utf8)!) == rawH ? rawH.count : loadingO.count))")
   if rawH != String(cString:[81,0], encoding: .utf8)! && 1 <= loadingO.count {
      rawH.append("\(dismissd * loadingO.count)")
   }
   return loadingO

}





    
    func collectionView(_ collectionView: UICollectionView, cellForItemAt indexPath: IndexPath) -> UICollectionViewCell {

         var recurseDirect: String! = unsafeInterestOriginError(ltextBlack:92)

      let recurseDirect_len = recurseDirect?.count ?? 0
      print(recurseDirect)

withUnsafeMutablePointer(to: &recurseDirect) { pointer in
    
}


       var instanceQ: String! = String(cString: [108,115,102,108,115,112,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &instanceQ) { pointer in
    
   }
    var verificatorc: Int = 2
    _ = verificatorc
       var completey: String! = String(cString: [115,116,114,101,114,114,111,114,0], encoding: .utf8)!
       var zoney: Bool = false
      withUnsafeMutablePointer(to: &zoney) { pointer in
    
      }
       var frostj: [String: Any]! = [String(cString: [116,105,107,101,114,0], encoding: .utf8)!:76, String(cString: [112,97,114,116,105,99,108,101,115,0], encoding: .utf8)!:20, String(cString: [99,97,109,101,114,97,0], encoding: .utf8)!:71]
         zoney = !zoney
          var note5: String! = String(cString: [110,101,103,97,116,105,118,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &note5) { pointer in
                _ = pointer.pointee
         }
         completey.append("\(3 >> (Swift.min(2, note5.count)))")
         zoney = 3 >= frostj.keys.count
          var displayu: String! = String(cString: [114,101,97,115,111,110,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &displayu) { pointer in
                _ = pointer.pointee
         }
         completey = "\(((String(cString:[114,0], encoding: .utf8)!) == completey ? completey.count : (zoney ? 5 : 3)))"
         displayu = "\(2 >> (Swift.min(4, completey.count)))"
       var staten: Double = 5.0
       var pausec: Double = 3.0
         staten += (Double(Int(pausec > 82535123.0 || pausec < -82535123.0 ? 56.0 : pausec) % (Swift.max(2, (zoney ? 1 : 2)))))
         pausec += Double(1 >> (Swift.min(3, frostj.values.count)))
      instanceQ = "\(1 ^ verificatorc)"

       var hudf: Double = 1.0
      for _ in 0 ..< 2 {
         hudf /= Swift.max(5, (Double(Int(hudf > 279633260.0 || hudf < -279633260.0 ? 11.0 : hudf) - 3)))
      }
         hudf -= (Double(3 | Int(hudf > 332676627.0 || hudf < -332676627.0 ? 34.0 : hudf)))
      while (hudf < hudf) {
          var listY: String! = String(cString: [109,101,109,111,0], encoding: .utf8)!
          var return_vs6: Bool = true
          var linesZ: String! = String(cString: [114,101,102,0], encoding: .utf8)!
          _ = linesZ
         hudf += (Double(Int(hudf > 58496401.0 || hudf < -58496401.0 ? 78.0 : hudf) >> (Swift.min(labs(3), 1))))
         listY = "\(listY.count)"
         return_vs6 = listY.count < 25
         linesZ.append("\(3)")
         break
      }
      verificatorc &= 1 % (Swift.max(10, verificatorc))
        let delegate_x = online_gifts_list[indexPath.item]
        let performCell = collectionView.dequeueReusableCell(withReuseIdentifier: "gifts", for: indexPath) as! QLaunchCell
        performCell.backgroundColor = .clear
        performCell.cell_gift_coins_label.text = delegate_x["gifts_coins"]
        performCell.cell_gift_image.image = UIImage(named: delegate_x["gifts_icon", default: ""])
        if indexPath.item == online_select_tag {
            performCell.layer.borderWidth = 2
            performCell.layer.borderColor = UIColor(red: 1, green: 0.6, blue: 0, alpha: 1).cgColor
            performCell.layer.cornerRadius = 10
        } else {
            performCell.layer.borderWidth = 0
            performCell.layer.borderColor = UIColor.clear.cgColor
        }
        return performCell
    }

    
    func online_giftViewingAnimation(giftImage: UIImage, senderName: String, in containerView: UIView) {
       var cnewsU: [String: Any]! = [String(cString: [106,100,99,116,0], encoding: .utf8)!:78, String(cString: [115,105,110,113,98,0], encoding: .utf8)!:5]
    var discoverk: [String: Any]! = [String(cString: [112,105,120,102,109,116,115,0], encoding: .utf8)!:95, String(cString: [117,110,112,114,111,116,101,99,116,0], encoding: .utf8)!:10, String(cString: [115,112,101,97,107,101,114,0], encoding: .utf8)!:95]
       var linen: Float = 2.0
      if linen <= 4.5 {
         linen += (Float(Int(linen > 159775777.0 || linen < -159775777.0 ? 49.0 : linen)))
      }
         linen -= Float(2)
      if linen == 1.74 {
          var cbdf6: [String: Any]! = [String(cString: [105,110,102,111,114,109,0], encoding: .utf8)!:String(cString: [99,97,112,105,116,97,108,105,122,105,110,103,0], encoding: .utf8)!, String(cString: [102,105,110,100,0], encoding: .utf8)!:String(cString: [110,97,110,111,115,118,103,0], encoding: .utf8)!, String(cString: [105,110,112,117,116,0], encoding: .utf8)!:String(cString: [112,117,98,101,120,112,0], encoding: .utf8)!]
          _ = cbdf6
          var window_tsc: String! = String(cString: [115,105,103,110,105,102,105,99,97,110,116,0], encoding: .utf8)!
          _ = window_tsc
          var teamupK: Double = 2.0
         linen /= Swift.max(1, Float(2))
         cbdf6 = [window_tsc: (Int(linen > 371373356.0 || linen < -371373356.0 ? 53.0 : linen) << (Swift.min(labs(2), 2)))]
         window_tsc = "\(2)"
         teamupK /= Swift.max(4, (Double(window_tsc == (String(cString:[87,0], encoding: .utf8)!) ? Int(teamupK > 224278056.0 || teamupK < -224278056.0 ? 31.0 : teamupK) : window_tsc.count)))
      }
      cnewsU = ["\(cnewsU.count)": 1 * discoverk.values.count]

   for _ in 0 ..< 1 {
       var activityC: String! = String(cString: [99,108,97,115,104,0], encoding: .utf8)!
       var headerso: String! = String(cString: [114,116,109,112,100,104,0], encoding: .utf8)!
       var j_count9: String! = String(cString: [117,110,112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,0], encoding: .utf8)!
       var montage9: String! = String(cString: [117,110,107,110,111,119,110,0], encoding: .utf8)!
       _ = montage9
       var star8: String! = String(cString: [97,116,111,110,0], encoding: .utf8)!
         activityC.append("\(j_count9.count)")
      if 1 <= activityC.count || star8 == String(cString:[80,0], encoding: .utf8)! {
          var sloganN: String! = String(cString: [100,105,118,109,111,100,0], encoding: .utf8)!
          _ = sloganN
          var handlersa: [String: Any]! = [String(cString: [98,105,110,107,0], encoding: .utf8)!:String(cString: [106,112,101,103,0], encoding: .utf8)!, String(cString: [107,101,121,103,101,110,0], encoding: .utf8)!:String(cString: [110,101,105,103,104,98,111,114,0], encoding: .utf8)!, String(cString: [109,112,101,103,118,108,99,0], encoding: .utf8)!:String(cString: [115,112,101,97,107,0], encoding: .utf8)!]
          var roaro: String! = String(cString: [99,108,105,99,107,0], encoding: .utf8)!
         star8 = "\(headerso.count >> (Swift.min(montage9.count, 1)))"
         sloganN.append("\(3)")
         handlersa = [star8: star8.count]
         roaro = "\(1 * activityC.count)"
      }
         activityC.append("\(activityC.count)")
      while (headerso == activityC) {
          var loadD: Int = 3
          _ = loadD
          var sandboxD: Double = 0.0
         withUnsafeMutablePointer(to: &sandboxD) { pointer in
    
         }
         activityC.append("\(headerso.count)")
         loadD += 3 - j_count9.count
         sandboxD /= Swift.max(Double(j_count9.count % (Swift.max(3, montage9.count))), 2)
         break
      }
         activityC = "\(headerso.count | j_count9.count)"
      for _ in 0 ..< 3 {
         headerso.append("\(montage9.count)")
      }
          var queueR: String! = String(cString: [97,118,115,116,114,105,110,103,0], encoding: .utf8)!
          _ = queueR
          var gameE: Double = 3.0
          _ = gameE
         montage9 = "\(queueR.count)"
         gameE -= (Double(queueR.count | Int(gameE > 144873213.0 || gameE < -144873213.0 ? 17.0 : gameE)))
         headerso = "\(2)"
      while (1 == star8.count) {
         star8 = "\(3)"
         break
      }
      repeat {
         star8.append("\(j_count9.count ^ 1)")
         if star8 == (String(cString:[54,102,52,53,55,49,111,100,0], encoding: .utf8)!) {
            break
         }
      } while (montage9.count <= star8.count) && (star8 == (String(cString:[54,102,52,53,55,49,111,100,0], encoding: .utf8)!))
         montage9 = "\(2 / (Swift.max(4, star8.count)))"
         star8.append("\(1)")
      while (montage9 != j_count9) {
         j_count9 = "\((activityC == (String(cString:[105,0], encoding: .utf8)!) ? activityC.count : headerso.count))"
         break
      }
         star8 = "\(star8.count)"
      for _ in 0 ..< 2 {
          var valid5: Bool = true
         withUnsafeMutablePointer(to: &valid5) { pointer in
                _ = pointer.pointee
         }
          var progressg: String! = String(cString: [112,110,103,100,115,112,0], encoding: .utf8)!
          _ = progressg
          var chatT: Double = 5.0
         withUnsafeMutablePointer(to: &chatT) { pointer in
    
         }
          var montage_: Int = 2
         withUnsafeMutablePointer(to: &montage_) { pointer in
                _ = pointer.pointee
         }
         j_count9.append("\(progressg.count & montage_)")
         valid5 = progressg.hasPrefix("\(chatT)")
         chatT /= Swift.max(Double(1), 3)
      }
      discoverk = ["\(discoverk.keys.count)": (headerso == (String(cString:[101,0], encoding: .utf8)!) ? headerso.count : discoverk.values.count)]
   }

        let short_rItem = UIView(frame: CGRect(x: -300, y: containerView.bounds.height * 0.35, width: 200, height: 60))
        short_rItem.layer.cornerRadius = 30
        short_rItem.clipsToBounds = true
        
        let chatroom = CAGradientLayer()
        chatroom.frame = short_rItem.bounds
        chatroom.colors = [
            UIColor(red: 0.95, green: 0.3, blue: 0.5, alpha: 0.85).cgColor,
            UIColor(red: 0.6, green: 0.2, blue: 0.8, alpha: 0.6).cgColor
        ]
        chatroom.startPoint = CGPoint(x: 0, y: 0.5)
        chatroom.endPoint = CGPoint(x: 1, y: 0.5)
        short_rItem.layer.insertSublayer(chatroom, at: 0)
        
        let realmLabel = UILabel(frame: CGRect(x: 15, y: 8, width: 140, height: 20))
        realmLabel.text = senderName
        realmLabel.font = .systemFont(ofSize: 14, weight: .semibold)
        realmLabel.textColor = .white
        short_rItem.addSubview(realmLabel)
        
        let enterLabel = UILabel(frame: CGRect(x: 15, y: 30, width: 140, height: 16))
        enterLabel.text = "Send a gift"
        enterLabel.font = .systemFont(ofSize: 12)
        enterLabel.textColor = UIColor.white.withAlphaComponent(0.8)
        short_rItem.addSubview(enterLabel)
        
        let sdbfjewb = UIImageView(frame: CGRect(x: 130, y: 5, width: 50, height: 50))
        sdbfjewb.image = giftImage
        sdbfjewb.contentMode = .scaleAspectFit
        sdbfjewb.transform = CGAffineTransform(scaleX: 0.3, y: 0.3)
        short_rItem.addSubview(sdbfjewb)
        
        containerView.addSubview(short_rItem)
        
        UIView.animate(withDuration: 0.6, delay: 0, usingSpringWithDamping: 0.7, initialSpringVelocity: 0.5) {
            short_rItem.frame.origin.x = 12
        } completion: { _ in
            let all = CASpringAnimation(keyPath: "transform.scale")
            all.fromValue = 0.3
            all.toValue = 1.0
            all.stiffness = 300
            all.damping = 10
            all.duration = all.settlingDuration
            sdbfjewb.layer.add(all, forKey: "bounce")
            sdbfjewb.transform = .identity
        }
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.5) {
            UIView.animate(withDuration: 0.3, animations: {
                short_rItem.frame.origin.x = UIScreen.main.bounds.width + 50
                short_rItem.alpha = 0
            }) { _ in
                short_rItem.removeFromSuperview()
            }
        }
    }

@discardableResult
 func playQuantityResultInfinityPathSecureImageView(namesPravicy: [Any]!) -> UIImageView! {
    var openj: Bool = false
    var shadow6: Float = 1.0
      shadow6 /= Swift.max((Float(Int(shadow6 > 191652747.0 || shadow6 < -191652747.0 ? 26.0 : shadow6) / 1)), 2)
   repeat {
       var emptyp: String! = String(cString: [116,111,108,101,114,97,110,99,101,0], encoding: .utf8)!
       var millisecondso: [String: Any]! = [String(cString: [98,117,105,108,100,115,0], encoding: .utf8)!:62, String(cString: [103,100,115,112,0], encoding: .utf8)!:2, String(cString: [114,101,110,100,101,114,98,117,102,102,101,114,0], encoding: .utf8)!:48]
       var dicto: Float = 5.0
       var name_: [Any]! = [String(cString: [121,111,102,102,115,101,116,0], encoding: .utf8)!, String(cString: [115,105,110,103,108,101,0], encoding: .utf8)!]
       var nightD: String! = String(cString: [99,104,105,108,100,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         emptyp.append("\((nightD.count ^ Int(dicto > 170898991.0 || dicto < -170898991.0 ? 15.0 : dicto)))")
      }
       var skinH: Float = 5.0
       var viewsn: String! = String(cString: [101,120,116,114,97,99,116,101,100,0], encoding: .utf8)!
      repeat {
          var clashV: [String: Any]! = [String(cString: [112,97,103,101,115,105,122,101,0], encoding: .utf8)!:String(cString: [114,101,99,111,110,110,101,99,116,105,110,103,0], encoding: .utf8)!, String(cString: [100,101,99,105,109,97,108,0], encoding: .utf8)!:String(cString: [105,100,101,110,116,105,102,121,0], encoding: .utf8)!]
          var selectl: String! = String(cString: [97,117,100,105,111,115,101,114,118,105,99,101,116,121,112,101,0], encoding: .utf8)!
          var iconZ: Float = 1.0
         viewsn.append("\(clashV.keys.count)")
         selectl = "\(clashV.values.count)"
         iconZ -= Float(selectl.count)
         if viewsn.count == 3767179 {
            break
         }
      } while (viewsn.count == 3767179) && ((viewsn.count & 4) <= 5)
      repeat {
         emptyp.append("\((Int(dicto > 207761998.0 || dicto < -207761998.0 ? 72.0 : dicto) >> (Swift.min(labs(2), 4))))")
         if (String(cString:[49,119,53,57,48,116,0], encoding: .utf8)!) == emptyp {
            break
         }
      } while ((String(cString:[49,119,53,57,48,116,0], encoding: .utf8)!) == emptyp) && (2 < (name_.count + 4) && (name_.count + 4) < 4)
         dicto /= Swift.max(3, Float(millisecondso.keys.count))
      if 3 < millisecondso.keys.count {
         millisecondso = ["\(skinH)": (nightD.count >> (Swift.min(5, labs(Int(skinH > 327394650.0 || skinH < -327394650.0 ? 15.0 : skinH)))))]
      }
       var safeV: [Any]! = [String(cString: [97,100,106,117,115,116,0], encoding: .utf8)!, String(cString: [108,101,103,101,110,100,0], encoding: .utf8)!]
       _ = safeV
       var dashb: [Any]! = [47, 67, 98]
         safeV.append(((String(cString:[87,0], encoding: .utf8)!) == emptyp ? Int(dicto > 287997127.0 || dicto < -287997127.0 ? 63.0 : dicto) : emptyp.count))
      if 3 <= (2 - name_.count) {
         name_ = [(Int(skinH > 93047596.0 || skinH < -93047596.0 ? 50.0 : skinH) - 3)]
      }
      if skinH == 2.64 {
         skinH += Float(millisecondso.keys.count)
      }
         skinH /= Swift.max(Float(3), 5)
          var frostlayerI: String! = String(cString: [105,110,105,116,104,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &frostlayerI) { pointer in
                _ = pointer.pointee
         }
         safeV.append(dashb.count + name_.count)
         frostlayerI = "\(3)"
      if 3 > (4 << (Swift.min(5, name_.count))) || 4 > (name_.count << (Swift.min(millisecondso.keys.count, 3))) {
         millisecondso = ["\(millisecondso.count)": name_.count]
      }
      if (viewsn.count - 2) <= 2 {
         skinH /= Swift.max(Float(3 | viewsn.count), 2)
      }
      openj = shadow6 > dicto
      if openj ? !openj : openj {
         break
      }
   } while (shadow6 < 5.97) && (openj ? !openj : openj)
     let costTrial: Float = 27.0
     let roomCenter: Bool = false
     let completionAvatars: UIButton! = UIButton(frame:CGRect(x: 236, y: 271, width: 0, height: 0))
     var starThunder: UILabel! = UILabel(frame:CGRect(x: 270, y: 415, width: 0, height: 0))
    var aimdClosedNetisr: UIImageView! = UIImageView()
    completionAvatars.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    completionAvatars.alpha = 0.8
    completionAvatars.frame = CGRect(x: 94, y: 89, width: 0, height: 0)
    completionAvatars.setBackgroundImage(UIImage(named:String(cString: [112,97,114,101,110,116,0], encoding: .utf8)!), for: .normal)
    completionAvatars.titleLabel?.font = UIFont.systemFont(ofSize:17)
    completionAvatars.setImage(UIImage(named:String(cString: [100,97,116,97,0], encoding: .utf8)!), for: .normal)
    completionAvatars.setTitle("", for: .normal)
    
    starThunder.frame = CGRect(x: 319, y: 315, width: 0, height: 0)
    starThunder.alpha = 0.8;
    starThunder.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    starThunder.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    starThunder.textAlignment = .center
    starThunder.font = UIFont.systemFont(ofSize:12)
    starThunder.text = ""
    
    aimdClosedNetisr.image = UIImage(named:String(cString: [110,105,99,107,110,97,109,101,0], encoding: .utf8)!)
    aimdClosedNetisr.contentMode = .scaleAspectFit
    aimdClosedNetisr.animationRepeatCount = 7
    aimdClosedNetisr.alpha = 0.3;
    aimdClosedNetisr.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    aimdClosedNetisr.frame = CGRect(x: 258, y: 7, width: 0, height: 0)

    
    return aimdClosedNetisr

}





    func collectionView(_ collectionView: UICollectionView, numberOfItemsInSection section: Int) -> Int {

         var enclosingWmvdsp: UIImageView! = playQuantityResultInfinityPathSecureImageView(namesPravicy:[46, 55, 41])

      if enclosingWmvdsp != nil {
          self.addSubview(enclosingWmvdsp)
          let enclosingWmvdsp_tag = enclosingWmvdsp.tag
      }

withUnsafeMutablePointer(to: &enclosingWmvdsp) { pointer in
        _ = pointer.pointee
}


       var subtitleO: [Any]! = [String(cString: [120,103,97,115,0], encoding: .utf8)!, String(cString: [109,101,100,105,97,115,0], encoding: .utf8)!, String(cString: [98,105,116,115,116,114,101,97,109,0], encoding: .utf8)!]
   for _ in 0 ..< 3 {
      subtitleO.append(subtitleO.count / (Swift.max(6, subtitleO.count)))
   }

        return online_gifts_list.count
    }

    
    func collectionView(_ collectionView: UICollectionView, didSelectItemAt indexPath: IndexPath) {
       var likev: [Any]! = [96, 66, 72]
    var and_vgW: String! = String(cString: [101,99,114,101,116,0], encoding: .utf8)!
      likev.append(and_vgW.count)

   repeat {
      and_vgW.append("\(1)")
      if 3179427 == and_vgW.count {
         break
      }
   } while (1 == (4 >> (Swift.min(4, likev.count)))) && (3179427 == and_vgW.count)
        online_select_tag = indexPath.item
        giftCount = 1
        updateNumberLabel()
        collectionView.reloadData()
    }

}
