
import UIKit

import Foundation

class MXNPurchases: NSObject {
private var recordMin: Double? = 0.0
private var fieldSpace: Double? = 0.0
private var fadeCoordinatordispatcher_string: String?




    let appStoreReceiptURL: URL?
    init(appStoreReceiptURL: URL? = Bundle.main.appStoreReceiptURL) {
        self.appStoreReceiptURL = appStoreReceiptURL
    }

    var appStoreReceiptData: Data? {
       var introj: [Any]! = [String(cString: [105,110,100,105,99,101,115,0], encoding: .utf8)!, String(cString: [119,122,97,101,115,0], encoding: .utf8)!]
   repeat {
       var arenal: Bool = true
      if !arenal {
         arenal = !arenal || !arenal
      }
      repeat {
          var followV: String! = String(cString: [97,98,108,0], encoding: .utf8)!
          _ = followV
          var covertk: Double = 3.0
          var wildforgei: Double = 4.0
          var epicm: [String: Any]! = [String(cString: [98,121,116,101,115,116,114,101,97,109,0], encoding: .utf8)!:1.0]
         withUnsafeMutablePointer(to: &epicm) { pointer in
                _ = pointer.pointee
         }
          var appl: Float = 3.0
         arenal = (String(cString:[84,0], encoding: .utf8)!) == followV
         covertk /= Swift.max(1, Double(epicm.keys.count))
         wildforgei -= (Double(2 & Int(wildforgei > 52153133.0 || wildforgei < -52153133.0 ? 35.0 : wildforgei)))
         epicm["\(appl)"] = (1 << (Swift.min(labs(Int(appl > 274076563.0 || appl < -274076563.0 ? 91.0 : appl)), 4)))
         if arenal ? !arenal : arenal {
            break
         }
      } while (arenal ? !arenal : arenal) && (arenal)
         arenal = !arenal
      introj.append((1 - (arenal ? 4 : 4)))
      if 317386 == introj.count {
         break
      }
   } while (317386 == introj.count) && (2 < (introj.count / (Swift.max(introj.count, 7))))

        guard let receiptDataURL = appStoreReceiptURL,
            let chimi = try? Data(contentsOf: receiptDataURL) else {
            return nil
        }
        return chimi
    }

    private var receiptRefreshRequest: IATMomentVerificator?

     
    


@discardableResult
 func submitUnknownBeforeKit() -> Double {
    var listT: String! = String(cString: [101,105,116,104,101,114,0], encoding: .utf8)!
    _ = listT
    var cellO: [String: Any]! = [String(cString: [108,97,98,101,108,0], encoding: .utf8)!:95, String(cString: [98,108,97,107,101,0], encoding: .utf8)!:65]
      listT = "\(listT.count)"
      listT.append("\(listT.count)")
   while ((4 >> (Swift.min(5, cellO.count))) < 5 && (cellO.count >> (Swift.min(labs(4), 4))) < 4) {
       var thunderv: Bool = false
       var mine7: Float = 2.0
         mine7 -= (Float(3 + Int(mine7 > 205854938.0 || mine7 < -205854938.0 ? 15.0 : mine7)))
          var createR: Double = 3.0
         thunderv = 42.48 <= createR
         mine7 -= (Float(Int(mine7 > 99412373.0 || mine7 < -99412373.0 ? 66.0 : mine7) | (thunderv ? 4 : 2)))
      repeat {
         thunderv = mine7 >= 44.94 || thunderv
         if thunderv ? !thunderv : thunderv {
            break
         }
      } while (thunderv ? !thunderv : thunderv) && (5.42 <= (mine7 + 5.94) || thunderv)
         thunderv = 87.31 > mine7 && thunderv
          var publisherz: String! = String(cString: [114,111,117,110,100,117,112,0], encoding: .utf8)!
          var frostbladeZ: Double = 2.0
         mine7 /= Swift.max(3, (Float(Int(mine7 > 59874753.0 || mine7 < -59874753.0 ? 18.0 : mine7) & publisherz.count)))
         frostbladeZ /= Swift.max((Double(2 / (Swift.max(7, (thunderv ? 5 : 4))))), 4)
      cellO["\(thunderv)"] = (Int(mine7 > 333353384.0 || mine7 < -333353384.0 ? 65.0 : mine7) + 2)
      break
   }
     let parentSign: String! = String(cString: [105,109,112,108,105,101,115,0], encoding: .utf8)!
    var nearestAlgor:Double = 0

    return nearestAlgor

}



@discardableResult
    public func fetchReceipt(forceRefresh: Bool,
                             refresh: IATMomentVerificator.ReceiptRefresh = IATMomentVerificator.refresh,
                             completion: @escaping (CClashH) -> Void) -> VPACInfoCover? {

         let processedPassf: Double = submitUnknownBeforeKit()

      print(processedPassf)

_ = processedPassf


       var codes: String! = String(cString: [108,97,114,103,101,0], encoding: .utf8)!
      codes.append("\((codes == (String(cString:[114,0], encoding: .utf8)!) ? codes.count : codes.count))")


        if let receiptData = appStoreReceiptData, forceRefresh == false {
            completion(.success(receiptData: receiptData))
            return nil
        } else {
            
            receiptRefreshRequest = refresh(nil) { result in
                
                self.receiptRefreshRequest = nil
                
                switch result {
                case .success:
                    if let receiptData = self.appStoreReceiptData {
                        completion(.success(receiptData: receiptData))
                    } else {
                        completion(.error(error: .noReceiptData))
                    }
                case .error(let e):
                    completion(.error(error: .networkError(error: e)))
                }
            }
            return receiptRefreshRequest
        }
    }
    
     
    
@discardableResult
    public func verifyReceipt(using validator: BXAStrikeMoments,
                              forceRefresh: Bool,
                              refresh: IATMomentVerificator.ReceiptRefresh = IATMomentVerificator.refresh,
                              completion: @escaping (CZoneBattleW) -> Void) -> VPACInfoCover? {
       var postw: String! = String(cString: [103,111,116,0], encoding: .utf8)!
   repeat {
       var jebdferta2: String! = String(cString: [115,99,97,108,101,109,111,100,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &jebdferta2) { pointer in
             _ = pointer.pointee
      }
       var builderl: Bool = true
       var parametersk: Double = 4.0
       _ = parametersk
      repeat {
          var queues: String! = String(cString: [98,105,116,0], encoding: .utf8)!
          var media6: String! = String(cString: [109,97,116,120,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &media6) { pointer in
                _ = pointer.pointee
         }
         parametersk += Double(jebdferta2.count)
         queues.append("\(queues.count)")
         media6.append("\(queues.count)")
         if parametersk == 543756.0 {
            break
         }
      } while (parametersk < 5.61) && (parametersk == 543756.0)
          var moditya: String! = String(cString: [116,111,107,101,110,105,122,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &moditya) { pointer in
                _ = pointer.pointee
         }
          var progress3: Double = 3.0
         builderl = !builderl && parametersk <= 93.53
         moditya.append("\(((String(cString:[73,0], encoding: .utf8)!) == jebdferta2 ? (builderl ? 2 : 4) : jebdferta2.count))")
         progress3 -= (Double(Int(parametersk > 315652919.0 || parametersk < -315652919.0 ? 7.0 : parametersk)))
       var giftp: Double = 2.0
      for _ in 0 ..< 3 {
          var crimsonarkI: String! = String(cString: [112,97,105,114,105,110,103,115,0], encoding: .utf8)!
         parametersk -= (Double((builderl ? 4 : 4) - 2))
         crimsonarkI.append("\((1 * Int(giftp > 137810438.0 || giftp < -137810438.0 ? 62.0 : giftp)))")
      }
          var completelyp: Double = 1.0
         withUnsafeMutablePointer(to: &completelyp) { pointer in
    
         }
          var loop7: String! = String(cString: [114,103,116,99,0], encoding: .utf8)!
          _ = loop7
          var friend_s6h: String! = String(cString: [109,105,110,117,115,0], encoding: .utf8)!
         giftp -= (Double(loop7 == (String(cString:[104,0], encoding: .utf8)!) ? loop7.count : Int(completelyp > 60261895.0 || completelyp < -60261895.0 ? 97.0 : completelyp)))
         friend_s6h.append("\((2 / (Swift.max(2, (builderl ? 2 : 1)))))")
      for _ in 0 ..< 3 {
         giftp += (Double((builderl ? 2 : 4)))
      }
          var shadowm: Double = 5.0
          var rnewsE: Double = 3.0
          var iconY: Double = 4.0
         withUnsafeMutablePointer(to: &iconY) { pointer in
    
         }
         jebdferta2 = "\((1 & Int(parametersk > 348174987.0 || parametersk < -348174987.0 ? 85.0 : parametersk)))"
         shadowm /= Swift.max((Double(Int(iconY > 304619317.0 || iconY < -304619317.0 ? 46.0 : iconY) ^ Int(shadowm > 73331768.0 || shadowm < -73331768.0 ? 48.0 : shadowm))), 4)
         rnewsE -= (Double(Int(shadowm > 325379981.0 || shadowm < -325379981.0 ? 78.0 : shadowm) << (Swift.min(3, labs(1)))))
         iconY += (Double(Int(rnewsE > 204022285.0 || rnewsE < -204022285.0 ? 97.0 : rnewsE) | 2))
      while (builderl && 3.100 < (1.8 + parametersk)) {
         parametersk -= (Double(Int(giftp > 53850419.0 || giftp < -53850419.0 ? 18.0 : giftp) + 2))
         break
      }
      for _ in 0 ..< 3 {
         giftp /= Swift.max(Double(1), 4)
      }
      postw = "\(jebdferta2.count)"
      if (String(cString:[52,52,55,119,95,121,0], encoding: .utf8)!) == postw {
         break
      }
   } while (2 > postw.count) && ((String(cString:[52,52,55,119,95,121,0], encoding: .utf8)!) == postw)

        
        return fetchReceipt(forceRefresh: forceRefresh, refresh: refresh) { result in
            switch result {
            case .success(let receiptData):
                self.verify(receiptData: receiptData, using: validator, completion: completion)
            case .error(let error):
                completion(.error(error: error))
            }
        }
    }

    
     
    private func verify(receiptData: Data, using validator: BXAStrikeMoments, completion: @escaping (CZoneBattleW) -> Void) {
       var stare: Int = 4
   withUnsafeMutablePointer(to: &stare) { pointer in
          _ = pointer.pointee
   }
    var pixelU: [Any]! = [11, 91, 4]
   if !pixelU.contains { $0 as? Int == stare } {
      stare >>= Swift.min(labs(stare), 5)
   }

     
        validator.validate(receiptData: receiptData) { result in
       var codeN: Bool = true
      withUnsafeMutablePointer(to: &codeN) { pointer in
    
      }
       var offerq: String! = String(cString: [102,97,118,101,100,0], encoding: .utf8)!
       _ = offerq
       var datesi: [String: Any]! = [String(cString: [97,99,107,117,112,0], encoding: .utf8)!:70, String(cString: [109,100,99,118,0], encoding: .utf8)!:64, String(cString: [105,99,111,110,105,99,0], encoding: .utf8)!:26]
      for _ in 0 ..< 3 {
         datesi["\(offerq)"] = 2
      }
      while (4 >= offerq.count && codeN) {
         offerq = "\((offerq == (String(cString:[71,0], encoding: .utf8)!) ? offerq.count : (codeN ? 2 : 3)))"
         break
      }
      for _ in 0 ..< 1 {
          var safeW: Bool = true
          var periodC: String! = String(cString: [100,101,99,111,100,101,102,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &periodC) { pointer in
    
         }
          var teamup4: Double = 1.0
          var l_managerV: Bool = true
         withUnsafeMutablePointer(to: &l_managerV) { pointer in
    
         }
         offerq = "\((offerq == (String(cString:[114,0], encoding: .utf8)!) ? Int(teamup4 > 238221782.0 || teamup4 < -238221782.0 ? 11.0 : teamup4) : offerq.count))"
         safeW = l_managerV && !safeW
         periodC = "\(((codeN ? 2 : 5) / (Swift.max(3, 1))))"
         l_managerV = !periodC.hasPrefix("\(teamup4)")
      }
         datesi = [offerq: ((codeN ? 1 : 3))]
      if codeN && 2 <= (datesi.keys.count / (Swift.max(5, 3))) {
          var generator_: String! = String(cString: [112,105,99,107,105,110,116,101,114,0], encoding: .utf8)!
          var followerlistY: Int = 2
         withUnsafeMutablePointer(to: &followerlistY) { pointer in
    
         }
          var invalidO: [Any]! = [false]
          var discoverd: Double = 5.0
         withUnsafeMutablePointer(to: &discoverd) { pointer in
                _ = pointer.pointee
         }
         codeN = (28 < (generator_.count * (!codeN ? 28 : generator_.count)))
         followerlistY -= (2 * Int(discoverd > 287264071.0 || discoverd < -287264071.0 ? 61.0 : discoverd))
         invalidO = [datesi.values.count]
         discoverd -= Double(followerlistY | 2)
      }
         datesi["\(codeN)"] = ((codeN ? 4 : 3) + offerq.count)
      repeat {
         offerq = "\((1 / (Swift.max(5, (codeN ? 4 : 5)))))"
         if (String(cString:[98,115,99,48,51,108,0], encoding: .utf8)!) == offerq {
            break
         }
      } while ((String(cString:[98,115,99,48,51,108,0], encoding: .utf8)!) == offerq) && (4 == (offerq.count ^ datesi.count))
          var homeY: String! = String(cString: [100,101,112,114,101,99,105,97,116,101,100,104,0], encoding: .utf8)!
          var watchdogW: String! = String(cString: [114,101,111,114,100,101,114,101,100,0], encoding: .utf8)!
          var infoJ: [String: Any]! = [String(cString: [105,110,100,105,99,116,111,114,0], encoding: .utf8)!:10, String(cString: [110,98,112,112,0], encoding: .utf8)!:67]
         offerq = "\(datesi.values.count << (Swift.min(labs(2), 5)))"
         homeY = "\(infoJ.count)"
         watchdogW = "\(2)"
         infoJ["\(codeN)"] = infoJ.count
      while (!offerq.hasPrefix("\(codeN)")) {
         codeN = datesi.values.count >= 55
         break
      }
      pixelU = [pixelU.count]
            
            DispatchQueue.main.async {
                completion(result)
            }
        }
    }
}
